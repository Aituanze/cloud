# Промпт: Добавить фичу в карточку TikTok-фида

Добавь **[ОПИСАНИЕ ФИЧИ]** в карточки TikTok-фида (Screen 3).

**Где код:**
- Карточка строится в `buildCard(l, index, total)` в `projects/agnt-24-app/js/app.js`
- Стили: секция `/* SCREEN 3 — FEED */` в `css/styles.css`
- Данные из объекта `l` (поля — см. listings.js)

**Дизайн-ограничения:**
- Высоту карточки не менять (scroll-snap y mandatory — сломается скролл)
- Тёплый серый для текста (var(--ink2) / var(--ink3)), не #000
- Цифры: Inter 700. Текст: Inter 500–600
- Не добавлять блоки между fc-photo и fc-content без крайней необходимости

**Фича:**
- Показывать: [что именно показывать]
- Условие: [когда показывать — для всех карточек / только active / только archive]
- Данные из поля: `l.[fieldName]` (убедись что поле есть в listings.js)
- Позиция в карточке: [fc-price-section / fc-badges-row / fc-meta / fc-stats-row]
