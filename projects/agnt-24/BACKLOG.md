# BACKLOG — agnt.24

## IN PROGRESS

- [ ] Баг-репорт владельца: «не вижу фото объектов в районах» — root cause
      найден (79% объявлений в проде, 100% Талгарского — без фото, т.к.
      `enrich_details()` дорогой по капче и не в cron, накопился долг по
      мере ежедневного пайплайна + массового сбора Талгара). Запущен
      `inbox/krisha-parser/enrich_photos_owner.py` (2601 owner-объявлений
      без фото, ~5с/запрос, идемпотентно `WHERE photos IS NULL`, лог —
      `enrich_photos_log.txt`) — фоном 2026-07-07, займёт ~3.5-4 часа.
      После завершения: пересобрать `listings.js` (build_app_data.py) и
      задеплоить.

## TODO

### Фаза 1 — Автопайплайн
_(готово — см. DONE)_

### Фаза 2 — Деплой каталогов
_(готово — см. DONE)_

### Фаза 3 — CRM
- [ ] Создать сервисный аккаунт Google Cloud → скачать `credentials.json`
- [ ] Установить `USE_GOOGLE_SHEETS=true` в `.env` бота
- [ ] Проверить, что лиды пишутся в Google Sheets
- [ ] Настроить еженедельный отчёт: `python reporter.py` по расписанию

### Фаза 4 — Масштабирование (Месяц 2–3)
- [ ] AI-теги фото (Google Vision API)
- [x] SEO для realty-quick-match (мета-теги, Open Graph) — см. DONE 2026-07-07

### Фаза 5 — Автозаполнение объявлений (ilvo.pro-style, кнопка $)
- [ ] **R&D**: как ilvo.pro постит на krisha.kz за агента — нужна авторизованная сессия
      агента на krisha.kz (логин/токен), реверс формы подачи объявления, обработка
      антибот/капчи (в парсере уже помечено — `enrich_details()` дорогой по капче)
- [ ] Кнопка "$" на объекте → автозаполнение фото/текста/параметров на основе editData
- [ ] Экран проверки перед публикацией (агент подтверждает, не публикует вслепую)
- [ ] Раз воркфлоу обкатан на krisha.kz — расширить на другие источники (olx.kz и т.п.)

### МОП: подтверждение переноса объектов (UI)
_(готово — см. DONE)_

### Приглашение агентов в агентство (invites)
_(готово — см. DONE)_

### Токены/бонусы/премии по рейтингу (из ТЗ владельца, отложено осознанно)
- [ ] Начисление токенов по рейтингу дисциплины/объёма
- [ ] Магазин токенов: буфет, апгрейды, пополнение баланса krisha.kz
- [ ] Премии/бонусы, назначаемые руководителем по рейтингу агента
      (сам владелец отметил: "будет дорабатываться по мере расширения")

### Дисциплина по Face ID (из ТЗ владельца, требует железа)
- [ ] Интеграция с Face ID на входе в офис → данные о приходе/уходе
- [ ] Расчёт рейтинга дисциплины на основе опозданий/пропусков по дням
      (сейчас в KPI-дашборде — заглушка "скоро: Face ID")

### Авто-расчёт вала/задатков вместо ручного ввода
- [ ] Сейчас `deposits_manual`/`volume_manual` — просто числа, которые
      руководитель редактирует руками (бэкфилл истории). Когда сделки
      начнут проходить через `leads`/`lead_events` в самом приложении —
      можно считать автоматически (`stage='deposit'`/`'deal'` + сумма
      по `properties.price`) и складывать с ручным бэкфиллом

### Возможности из исследования (2026-07-01)
- [ ] **Нативные push-уведомления** — ilvo работает только в браузере, это незакрытая ниша
- [x] **Kanban-воронка** (Новый лид → Контакт → Показ → Оффер → Сделка) — колонки по этапам
      уже реализованы (`agent-crm.js`); drag-and-drop жеста между колонками нет, смена
      этапа — кнопками в карточке лида (см. DONE 2026-07-07 «умные напоминания»)
- [x] **Умные напоминания** — «Вы не звонили клиенту N дней» с кнопкой «Позвонить» — см. DONE 2026-07-07
- [ ] **Поле депозит в парсере** — krisha.kz имеет поле залога (50 000 ₸) для аренды
      (заблокировано: у agnt.24 сейчас нет объявлений аренды вообще, парсер собирает
      только продажу — см. [[project_agnt24_app_state]])
- [ ] **Спарклайн динамики цены** в карточке объявления (тренд мирового рынка) — нужна
      история цен по объявлению, сейчас не трекается

## DONE

| Задача | Дата | Результат |
|--------|------|-----------|
| Баг-репорт владельца: «В базу» не отражался в «Мои объекты» | 2026-07-07 | Systematic-debugging: root cause — кнопка «В базу» видна на карте/ленте ВСЕМ (карта работает без авторизации по дизайну), но `_syncClaimToSupabase()` при отсутствии `window._agentProfile` молча делает `return` — UI при этом врал "✓ Вы взяли в работу" (и на экране редактирования объекта — "Сохранено ✓"), хотя запись в Supabase `properties` не происходила вообще. Т.к. «Мои объекты» читает ТОЛЬКО Supabase (`Sb.getAgentProperties`), такой claim НИКОГДА не появлялся там — ни сразу, ни после логина, ни когда-либо, независимо от того, залогинивался ли агент позже (ничего не запоминало, что claim не досослан). Разобрал не только сам клик по ленте, но и `saveEditListing()` на экране «Моя База» (тоже доступен без логина, тоже врал "Сохранено ✓"). Фикс в `app.js`: (1) клик «В базу» без `_agentProfile` теперь блокируется — тост «Войдите как агент, чтобы объект попал в «Мои объекты»» + `Auth.showAgentLogin()`, claim вообще не создаётся локально как fake-success; (2) `saveEditListing()` при отсутствии профиля честно говорит «Сохранено локально — войдите как агент…» вместо "Сохранено ✓"; (3) новый `_syncPendingClaims()` — при входе (боевой boot-флоу) досылает в Supabase любые claim'ы, оставшиеся в localStorage без `supabaseId` (закрывает уже возникшие из-за старого бага случаи, если такие есть у владельца прямо сейчас — не требует переklaim'ивать вручную); (4) реальная (не 23505) ошибка синка теперь тоже показывает тост вместо тихого `console.error`. Проверено в Node vm (полная симуляция `renderFeed`+клика по кнопке, не только вызов функций напрямую): гость не может создать fake-claim и получает промпт логина; агент создаёт claim и он реально долетает до `Sb.upsertProperty`; повторный `_syncPendingClaims()` не долбит уже синхронized объекты повторно. Живой Playwright не проверял (нет боевых учётных данных агента). Деплой success ✅ |
| SEO для realty-quick-match (мета-теги, Open Graph) | 2026-07-07 | Следующий пункт плана из «Фаза 4 — Масштабирование» в `projects/agnt-24/inbox/realty-quick-match/index.html`: `meta description`, `canonical` (реальный прод-URL `https://aituanze.github.io/cloud/quick-match/`, сверен по `.github/workflows/deploy-agnt-24.yml` — папка `inbox/realty-quick-match` копируется в `_site/quick-match`), полный набор Open Graph (`og:title/description/url/image/site_name/locale`) + Twitter Card `summary_large_image`, `theme-color` под навигационный `--navy` палитры приложения. `og:image`/`twitter:image` указывают на уже существующий реальный скриншот `shots/feed.png` (не заглушка). Фавикон — инлайн `data:image/svg+xml` (квадрат `--navy`+буквы «24» цветом `--accent`/champagne gold), не требует нового бинарного файла. До этого у страницы вообще не было ни meta description, ни OG-тегов, ни фавикона — превью ссылки в Telegram/WhatsApp показывало голый заголовок без картинки. Проверено: декодирован data-URI фавикона (валидный SVG), regex-проверка что `<head>` не сломан и все теги на месте (og:image/canonical присутствуют, href фавикона не оборвался посередине из-за кавычек). Деплой success ✅ |
| CRM: «умные напоминания» — «не звонили N дней» на карточке лида | 2026-07-07 | Реализация пункта из исследования конкурентов (2026-07-01, IRM365/Plecto/Lofty-паттерн — CRM подсвечивают застоявшиеся лиды). В `agent-crm.js`: `_daysSinceContact()` считает дни с последней заметки по лиду (или с момента создания лида, если заметок нет), `_isStale()` — порог 3 дня, не считая закрытые этапы «Сделка»/«Отказ». На карточке лида в kanban-доске — предупреждение «⚠️ Не звонили N дней/дня» (верная русская плюрализация) + кнопка быстрого звонка `tel:` прямо на карточке, без захода в детальный экран. Просроченные лиды сортируются в начало своей колонки (внутри застоявшихся — сначала самые давние). Обнаружил, что 2 других пункта того же списка исследования уже были реализованы ранее без отдельной пометки в бэклоге — сама kanban-доска по этапам (`crmBoard`/`STAGES`) и есть, только без drag-and-drop жеста; отметил это в TODO, чтобы не переоткрывать как новую задачу. Проверено в Node vm (5 сценариев: без заметок но недавно создан → не застоялся; заметка 1 день назад → не застоялся; этап «Сделка» без заметок 30 дней → не застоялся, закрытые сделки не мешают; 5 и 21 день без контакта → застоялся; плюрализация 1/2/5/11/21 → день/дня/дней/дней/день — верно; сортировка колонки — застоявшиеся первыми, внутри по убыванию дней). Живой Playwright не проверял — нет учётных данных агента (см. постоянное ограничение в [[project_agnt24_app_state]]), только логика и вёрстка карточки. Деплой ещё не выполнялся — ожидает git push. |
| Талгарский район: сузить до 6 посёлков + убрать сам город | 2026-07-07 | По просьбе владельца: в фильтре «Посёлок» оставить только Бесагаш/Туздыбастау/Бельбулак/Бирлик/Талдыбулак/Гульдала (было 7 — убраны Кендала/Актас/Алмалык/Панфилово/Отеген батыр), а данные и счётчики самого города Талгар (объекты без посёлка — просто улицы Талгара) убрать из приложения полностью. Проверил canonical location-dropdown krisha.kz на странице `/talgar/` — у каждого посёлка свой алиас (Туздыбастау→`tuzdybastau-kalinino`, Бельбулак→`belbulak-michurino`, Бирлик→`birlik`, Талдыбулак→`taldybulak`, Гульдала→`guldala`), добавлено по 5 персональных SEARCH_URLS на каждый (аналогично уже существующему Бесагашу); для объявлений с персональной ссылки посёлка microdistrict проставляется напрямую по URL-сегменту (`SETTLEMENT_MICRODISTRICT_OVERRIDE`), а не только текстовым поиском по адресу — надёжнее, раз название посёлка не всегда есть в самом адресе карточки. Заодно нашёл и починил баг в `parse_district()`: общегородской поиск «Алматы» на krisha.kz иногда отдаёт и пригородные объявления Талгарского района с адресом вида «пос. Гульдала»/«п. Бесагаш, …» без «р-н» в тексте — раньше такой сырой текст записывался как district напрямую (значение "пос. Гульдала" вместо "Талгарский"), и `build_app_data.py` тихо дропал такие строки как неизвестный район — 12 реальных объектов просто исчезали. Собрано +1257 новых объявлений (5 новых персональных ссылок + обновление существующих), бэкфилл 895 уже собранных строк под новый список посёлков (`backfill_talgar_settlements.py`), обогащение `seller_type` для всех 1234 новых Талгарских строк (`enrich_talgar_sellers.py`, фон ~2 часа с одним перезапуском после прерывания окружением — идемпотентно, дозавершилось без потерь: 2091 owner, 45 complex корректно исключены как ЖК-объявления застройщика). `build_app_data.py` теперь пропускает Талгарские строки без microdistrict (сам город). Итог в проде: Талгарский — 1197 активных + 28 архивных, только по 6 посёлкам (Бесагаш 112, Туздыбастау 339, Бельбулак 269, Бирлик 178, Талдыбулак 175, Гульдала 152), сумма по типам/режиму сделки/посёлкам перепроверена — везде сходится. Живой Playwright: фильтр показывает ровно нужные чипы посёлков (без единой уличной строки самого города), счётчики чипов совпадают с прямым подсчётом по данным, ошибок консоли/сети нет. Деплой success ✅ |
| Аудит остальных экранов, раунд 6: тихий сбой real-time опроса реальных claims коллег | 2026-07-07 | Прочитан `js/app.js` целиком (1567 строк, единственный крупный файл, ещё не разбиравшийся построчно). Нашёл реальный баг в `pollForFreshListings()` (2-минутный опрос `data/listings.js` для живого обновления карты/счётчиков): три ранних `return` внутри `try` (нет ответа сети, нет regex-совпадения, `freshListings.length===LISTINGS.length`) выходили из ФУНКЦИИ ЦЕЛИКОМ, минуя финальный безусловный вызов `App._loadAgencyClaims()` — то, что по комментарию в коде должно было держать свежей красную/зелёную лампочку «занято коллегой» (фикс от 07-06) на каждом тике. Самый частый случай — число объявлений НЕ меняется (claim коллеги пишется только в Supabase `properties`, вообще не трогает `listings.js`; новые листинги там появляются лишь по расписанию парсера) — то есть именно тот случай, где обновление claims нужнее всего, раньше почти всегда пропускался: real-time защита от дублей на практике работала только тогда, когда парсер СЛУЧАЙНО добавлял новый листинг в то же самое двухминутное окно. Фикс: убраны ранние return, логика обновления `LISTINGS`/`DISTRICTS` вложена в `if` вместо охранных `return` — `_loadAgencyClaims()` теперь гарантированно выполняется на каждом тике независимо от внутреннего результата. Проверено в Node vm-моках (обе ранее пропускавшиеся ветки — "список не изменился" и "сеть упала" — подтверждённо теперь тоже обновляют claims). Заодно убраны 3 мёртвые переменные, подсвеченные диагностикой редактора (`total` в `openDistrict`, `d` в `renderDistrictGrid`, `idx` в неактивном scroll-обработчике `.fc-counter`, который на каждый скролл переписывал уже верный статичный текст счётчика на то же самое значение — весь обработчик был инертным, удалён целиком). Живая проверка Playwright (iPhone 13, гостевой режим): карта → экран района → назад → вкладки Профиль/Моя База — без ошибок консоли и упавших запросов. Деплой success ✅ |
| Аудит остальных экранов, раунд 5: css/styles.css целиком | 2026-07-07 | По просьбе владельца — построчный аудит всего `css/styles.css` (1487 строк). Автоматизированный поиск точных дублей селекторов (`grep` по классам/id + `sort \| uniq -d`) нашёл 2: **`.colleague-row`** (реальный баг) и **`.sr-arrow`** (безобидный дубль). `.colleague-row` был определён дважды: старый мёртвый вариант (тёмный полупрозрачный фон/бордер/радиус/margin) и реально используемый в `transfer-ui.js` (список коллег для передачи объекта, чистый белый list-row). Т.к. CSS-каскад мёржит свойства блоков с одинаковым селектором, а не заменяет их целиком, непересекающиеся свойства мёртвого блока (фон/бордер/радиус/margin) просачивались в реальный компонент — визуальный баг просвечивания стилей. Заодно тем же мёртвым блоком шли `.col-info`/`.col-lbl`/`.col-name`, `.fix-btn`(+4 модификатора), `.swipe-hint`/`.swipe-arrow`(+`@keyframes bounce`)/`.swipe-txt` — все подтверждены неиспользуемыми (живые версии `.fc-fix-btn`/`.fc-swipe-hint` не задеты, у них отдельный корректный CSS). Удалены оба дубля (~33 строки), файл 1487→1454 строк. Построчно дочитан весь остальной файл — других багов не найдено. Проверено на проде: `curl` живого `styles.css` подтвердил ровно по одному вхождению `.colleague-row`/`.sr-arrow`, мёртвые правила отсутствуют. Деплой success ✅ |
| Удалить мёртвый supabase-config.js | 2026-07-07 | Прочитал `supabase-client.js` (уже проверен ранее) и оставшиеся мелкие JS-файлы целиком. `supabase-config.js` нигде не подключён (`index.html` не грузит его `<script>`, значения `SUPABASE_URL`/`SUPABASE_KEY` захардкожены отдельно прямо в `supabase-client.js`) — внутренний комментарий врал про «добавлен в .gitignore» (на деле не был). Удалён. Проверено локально и на проде: карта грузится, лента покупателя тянет реальные Supabase-данные — на работу приложения не повлияло. Деплой success ✅ |
| Аудит остальных экранов, раунд 4: тихий сбой перевода лида по этапам CRM | 2026-07-07 | Прочитал `supabase-client.js` целиком (263 строки) — единственная write-операция без проверки `error` во всём файле: `updateLeadStage()` (UPDATE leads + INSERT lead_events для смены этапа сделки Новый→Контакт→Показ→Задаток→Сделка). Если запись в БД падала (RLS/сеть), функция всё равно резолвилась успешно — `agent-crm.js` обновлял `lead.stage` локально и перерисовывал борд, как будто перенос состоялся, хотя в реальности этап не менялся: при перезагрузке или на другом устройстве лид откатывался обратно — тихое расхождение данных в самом важном месте воронки продаж. Добавлены `error`+`throw` в обе операции; `agent-crm.js` оборачивает вызов в try/catch — при ошибке toast с причиной, `lead.stage` не трогается и борд не перерисовывается. Проверено логикой (Node vm, симуляция сбоя) — `lead.stage` остаётся прежним при ошибке, toast показывается. Живой CRM-борд проверить не смог (нет учётных данных агента) — только этот фикс, остальной файл (все другие insert/update/delete/upsert) уже был корректен. Деплой success ✅ |
| Удалить мёртвый код Auth.showAgencySetup() | 2026-07-07 | Найден в предыдущем раунде аудита как недостижимый (никто не вызывает), удалён по просьбе владельца: функция, обработчики setupSubmit/setupToLogin, весь HTML screen-agency-setup. RLS (agency_hierarchy.sql) уже давно отозвала открытую INSERT-политику на agencies, так что живого риска не было — чистая гигиена кода. Проверено локально и на проде: логин/ошибка входа, ветка Покупатель→лента→OTP→Назад работают без сбоев, screen-agency-setup отсутствует в DOM. Деплой success ✅ |
| Аудит остальных экранов, раунд 3: коллизия чипов «Объекты» ↔ «Создать агентство» | 2026-07-07 | Экраны за логином (Объекты/Сделки-борд/Моя команда) живьём проверить не могу (нет учётных данных агента) — сделал систематическую сверку: (а) все id в index.html vs реальные ссылки в JS (11 «неиспользуемых» — все ложные срабатывания: querySelector-доступ, обёртки без прямой нужды в JS, SVG-градиент); (б) все переиспользуемые CSS-классы чипов (`.ed-chip`, `.pe-chip`, `.type-chip`) на предмет пересечения между независимыми формами. Нашёл реальный баг: `#peTypeChips` (тип объекта, редактор «Объекты») и `#anSubChips` (статус подписки, «Создать агентство» у суперадмина) используют ОДИН класс `.pe-chip` — `AgentProperties.init()/openEditor()/_save()` обращались к нему БЕЗ скоупа на контейнер (`document.querySelectorAll('.pe-chip')` глобально), из-за чего клик в одной форме гасил `.on` в другой (актуально для суперадмина, который одновременно агент — общий сценарий согласно текущей ролевой модели). HierarchyUI уже был правильно скоуплен, поправил AgentProperties. Заодно `_save()` глотал ошибки молча (try/finally без catch) — добавлен toast с сообщением об ошибке. `.ed-chip` (редактор объявления, 4 независимые группы) уже был корректно скоуплен везде — коллизий не найдено. Проверено логикой (изолированные массивы чипов не влияют друг на друга). Деплой success ✅ |
| Аудит остальных экранов, раунд 2: «Сохранённые» + OTP-экран покупателя | 2026-07-07 | (1) **«Сохранённые» была полностью нерабочей с момента создания** — вся инфраструктура (счётчик statSaved, группировка в «Моя База», подсказка «В ленте нажмите закладку») существовала, но `state.saved` нигде не записывался — в ленте не было ни одной кнопки-закладки. Добавлена кнопка-закладка на фото карточки (`fc-bookmark-btn`), тоггл + localStorage, тот же паттерн что у claimed. Проверено живьём: клик → счётчик в Профиле → карточка в «Моя База → Сохранённые». (2) **Экран ввода телефона (OTP) у покупателя просвечивал** — `Auth.showPhoneOTP()` намеренно открывается ПОВЕРХ текущего экрана (не прячет его, чтобы после OTP вернуться туда же), но `.screen` не имеет z-index — при position:absolute стек решается по порядку в DOM, а `screen-buyer-feed` идёт позже `screen-phone-otp` в index.html и перекрывал его. Добавлен `z-index:1` на `#screen-phone-otp`. Также live-проверка price-модала, тайм-табов (3 дня/Неделя/24ч — итог не меняется, меняется только «X новых», это по дизайну) и экрана редактирования объекта (claim→edit→save) — без багов. Проверено Playwright локально (до/после фикса z-index) и на проде. Деплой success ✅ |
| Талгарский район + фильтр по микрорайонам/посёлкам | 2026-07-07 | По просьбе владельца добавлен 9-й район «Талгарский» (пригород Алматинской области, не р-н города Алматы), настроенный так же, как остальные 8 — карта/счётчики/фильтры/лента/claim работают одинаково для любого района благодаря data-driven архитектуре. krisha.kz отдаёт Талгар и Бесагаш отдельными URL-сегментами (`/talgar/`, `/besagash-dzerzhinskoe/` — не просто "besagash", сверено по канонiческому списку н.п. Алматинской области на самом krisha.kz), не р-нами Алматы — новые SEARCH_URLS (5 категорий × 2 нас. пункта) + SETTLEMENT_DISTRICT_OVERRIDE (район по URL, не по адресу). Новое поле `microdistrict` — извлекается бесплатно при обычном парсинге списка (без похода на detail-страницу): «мкр X» regex для 8 р-нов Алматы (покрытие 3-82% в зависимости от застройки района), словарь реальных посёлков для Талгарского (Бесагаш/Кендала/Актас/Талдыбулак/Алмалык/Отеген батыр — то, что реально встречается в адресах; вглубь самого города Талгар не заходили, как и просил владелец). Собрано и проверено 895 объявлений — все от хозяев (0 агентских, 0 снято). Фронтенд: новая facet-секция «Микрорайон»/«Посёлок» на экране фильтра (заголовок меняется по району) — тот же паттерн real-time facet-пересчёта, что у «Год постройки»; SVG-пузырь + расширенный viewBox карты (без визуального разрыва фона/гор/сетки). Проверено на моках до сбора данных и живым Playwright на мобильном после — карта/район/фильтр/посёлки корректны, счётчики совпадают, ошибок нет. Деплой success ✅ |
| «Занято коллегой» не проверяло реальные claims агентства (только localStorage/демо) | 2026-07-06 | Владелец напомнил назначение «В базу»: показывать все объекты базы в клиентской TikTok-ленте + защита от дублей между риэлторами (красная/зелёная лампочка). Код-ревью всего пайплайна claim→Supabase→«Мои объекты»→публикация→buyer-feed показал: (1) claim→«Мои объекты» синхронизация архитектурно верна (`_syncClaimToSupabase` → `properties` таблица → `AgentProperties.renderList()` по agent_id при каждом открытии вкладки «Объекты») — живой multi-agent E2E не проверял (нет доступа к паролям боевых аккаунтов, см. [[project_agnt24_app_state]]). (2) **Реальный баг**: красная/зелёная лампочка на карточке типа (архив) и баннер «занято коллегой» в ленте проверяли только `state.claimed` (localStorage ЭТОГО телефона) или синтетические демо-данные (`l.claimedBy`, случайно проставленные в build_app_data.py для 25% архивных сэмплов) — для активных объявлений `l.claimedBy` всегда null, значит кнопка «В базу» никогда не блокировалась реальным claim'ом коллеги на другом устройстве, хотя `properties.source_krisha_id` уже писался при каждом claim. Фикс: `Sb.getAgencyClaimedKrishaIds()` + кэш `App._agencyClaims` (грузится при логине, обновляется 2-минутным polling наравне с listings.js) — `buildCard()`/`renderDistrictGrid()` теперь показывают РЕАЛЬНОЕ имя коллеги и блокируют кнопку, с приоритетом над архивным демо, если есть реальные данные. Плюс защита от гонки на клике + откат при конфликте (`_syncClaimToSupabase` ловит код `23505`) + `supabase/prevent_duplicate_claims.sql` — частичный уникальный индекс `(agency_id, source_krisha_id) WHERE status<>'archived'` на уровне БД (требует ручного запуска в Dashboard). Проверено в Node vm на 6 сценариях (свободно/моё/занято коллегой/архивное демо/реальные данные поверх демо/гостевой офлайн) — все верно; живой Playwright на мобильном подтвердил отсутствие регрессий в гостевом режиме (единственная ошибка — 404 фото с CDN krisha, не связана с фиксом). Деплой success. SQL-миграция `prevent_duplicate_claims.sql` накатана владельцем в Supabase Dashboard ✅ |
| Аудит остальных экранов (Аренда/Архив) — 3 бага в согласованности счётчиков | 2026-07-06 | По просьбе владельца («проверь остальные экраны на баги, окно аренды и архив тоже») найдено и исправлено: (1) **Вкладка «Аренда» показывала объекты продажи вместо пустого состояния** — `renderMap()` проверял несуществующее поле `l.dealType` (в данных этого поля нет вообще, deal-type хранится в `l.mode`), из-за чего карта в «Аренде» всегда показывала 0, а `getFilteredListings()`/`renderDistrictGrid()`/`_facetBase()` вообще не различали sale/rent — при заходе в район вылезали самые обычные объекты продажи (напр. 154 «объекта» в Бостандыкском на вкладке «Аренда», хотя реальных объявлений аренды в базе 0, парсер собирает только prodazha). Фикс: единый `App._matchesMode(l)`, сравнивающий `state.mode` напрямую с `l.mode`. (2) **Карта и карточки района не учитывали диапазон цены** (`renderMap()`/`renderDistrictGrid()`) — тот же класс бага, что раньше нашёлся в фильтрах чипов, разошлось на 1 объект (архив, дороже 200 млн ₸ учитывался картой, но не итогом «Найти»). (3) **Кнопки «Найти»/«Показать» при 0 результатах молча ничего не делали** — `openFeed()` тихо return'ился без перехода и без сообщения; особенно заметно стало ПОСЛЕ фикса (1), т.к. «Аренда» теперь почти всегда законно показывает 0. Добавлен toast «Ничего не найдено — попробуйте другие фильтры». Проверено Node-песочницей по всем 8 районам×5 типам×3 режимам (карта vs «Найти» — 15/15 совпадений, facet-регрессия sale+archive — 186 чипов/0 несовпадений) и живым Playwright на мобильном (iPhone 13 viewport) на проде — консоль чистая на всех экранах. Деплой success ✅ |
|--------|------|-----------|
| Фильтр «Год постройки» + faceted-пересчёт счётчиков на screen-filter | 2026-07-06 | По просьбе владельца — на экране фильтров района добавлен блок «Год постройки» (до 2000/2000–2009/2010–2019/2020+). Бэкенд: krisha.kz отдаёт год атрибутом `data-name="house.year"` на detail-странице (общий для квартир и домов) — `parse_year()`/`year_built` в схему БД, `enrich_details()` тянет его заодно с condition/photos для новых объявлений (без доп. запросов), `enrich_year_built()`/`--enrich-year` + разовый `backfill_year.py` для 990 уже собранных owner-объявлений (фон, ~85 мин): год найден у 678, 1 снято с публикации. После пересборки `listings.js` — 686 из 987 записей с `yearBuilt`. Мобильная Playwright-проверка (iPhone 13 viewport) живого прода нашла 2 реальных бага: (1) счётчики на ВСЕХ чипах фильтра (тип дома/комнатность/состояние/год) не учитывали `priceFrom`/`priceTo` — чип «2020+» показывал 12, а по факту при выборе получалось 10 (2 объекта дороже 200 млн исключались итоговым фильтром, но не счётчиком); (2) счётчики каждой группы считались один раз при открытии экрана и не пересчитывались с учётом уже выбранных чипов ДРУГИХ групп («не в реальном времени» — баг-репорт владельца). Оба фикса: цена добавлена в общий `base`; введён `_facetBase(excludeGroup)` — каждая группа теперь считается по всем АКТИВНЫМ фильтрам, кроме своей же, и полный `renderFilterContent()` вызывается заново при каждом клике по чипу; выбранный чип не пропадает из списка, даже если пересечение с другими фильтрами дало 0 (иначе его нельзя было бы снять). Проверено исчерпывающе по всем 8 районам × 5 типам: 186 одиночных чипов + 141 после live-клика + 200 комбинаций из нескольких групп — 0 несовпадений с независимой эталонной реализацией фильтра. Деплой success, перепроверено в браузере на мобильном виде ✅ |
| «Моя команда»: лидерборд агентов + средние показатели по команде | 2026-07-06 | По просьбе владельца («следующую сессию продолжим поле команды дальше») — конкурентный ресёрч 3 приложений (IRM365, Plecto, Lofty) показал общий паттерн: рейтинг/leaderboard агентов + сравнение со средним по команде мотивируют без доп. функционала. Добавлено в `hierarchy-ui.js` без изменений схемы БД: агенты в каждой группе МОПа и общий список у МОПа теперь отсортированы по `volume_manual` (вал) по убыванию; глобальный ранг в рамках зоны видимости (у директора — все агенты, у МОПа — только его) с медалями 🥇🥈🥉 для топ-3 (`.team-member-rank` в styles.css); новая KPI-секция «Средние показатели по агенту» (средний вал/задатки). Проверено в песочнице Node vm (мок-профили, сортировка/ранги/среднее посчитаны верно) — живой Supabase E2E не поднимал, т.к. тестовые аккаунты из прошлой сессии уже удалены ✅ |
| Чистка агентских объявлений из базы (баг-репорт: в «Объекты» не только хозяева) | 2026-07-04 | Причина: `das[who]=1` (коммит 00:42) фильтрует только НОВЫЕ объявления при парсинге, но 1243/1470 строк в `listings.db` собраны ДО фильтра без проверки продавца — поля для этого не было вообще. Плюс отдельный баг: `sellerType` в `build_app_data.py` был жёстко захардкожен `'owner'` для всех строк, бейдж «Хозяин» врал независимо от реальных данных. Добавлено: `seller_type`/`seller_agency` в схему БД, `parse_seller()` (вытаскивает `{"seller":{"type":...}}` из embedded JSON на странице объявления), `enrich_seller_type()`/`python krisha_parser.py --enrich-seller`. `build_app_data.py` теперь фильтрует `WHERE seller_type='owner'` и пишет реальное значение вместо хардкода. Полный прогон по всем 1470 записям (фон, ~5с/запрос): **990 хозяев, 470 агентских/застройщик** (339 агент, 91 застройщик, 40 агентство напрямую; топ — ИП Османов 34, DOMPRO.KZ 19, SWEETHOME 18, SunSales 16, Golden Time 16), 10 сняты с публикации. После пересборки `listings.js`: активных объявлений 1378→927 (убрано 451 нехозяйское). Деплой success ✅ |
| Фикс: тупик на экране «Приглашение не найдено» | 2026-07-04 | При переходе по невалидной/уже использованной ссылке `?invite=` экран показывал только текст ошибки без единого способа выйти на обычный логин (баг-репорт со скриншотом). Добавлена кнопка «← Войти в свой аккаунт» (`inviteToLogin` в index.html), появляется при ошибке в `InviteFlow._show()`, по клику убирает `?invite=` из URL (`history.replaceState`, без перезагрузки) и вызывает `Auth.showAgentLogin()`. Проверено Playwright (headless chromium) на локальном сервере: невалидный токен → кнопка видна → клик → экран `screen-auth` становится активным ✅ |
| Сквозной E2E-тест иерархии (superadmin→директор→МОП→агент) | 2026-07-04 | Через браузер (firecrawl) прогнана полная цепочка на тестовом агентстве «TAgnt»: директор зарегистрирован по инвайту, пригласил МОПа (заголовок формы верно показал «Пригласить МОПа»), МОП зарегистрирован и пригласил агента («Пригласить агента»), агент зарегистрирован. У директора в «Моя команда» отобразилось «МОП: Тест МОП (1)» с вложенным «Тест Агент» — иерархия и FK-фикс работают на всей цепочке, не только на логине. Побочно: одна из первых ссылок-приглашений была случайно создана на email реального директора (`legendazharokova@gmail.com`) — отменена до регистрации, не затронула боевые данные. Тестовые аккаунты и агентства («TAgnt», «TEST Claud Boss») удалены из Supabase скриптом `supabase/cleanup_e2e_test_accounts.sql`, итоговая проверка — `leftover 0` ✅ |
| Критичный фикс: логин не работал ни у кого после миграции иерархии агентств | 2026-07-04 | После `agency_hierarchy.sql` (добавил `agencies.created_by → profiles.id`) между `profiles`/`agencies` стало ДВЕ связи вместо одной — `Sb.getProfile()`'s `select('*, agencies(*))` стал неоднозначным для PostgREST и падал молча (профиль "не находился" вообще ни у кого, не только у superadmin — задевало и директора агентства `legendazharokova@gmail.com`). Раскопано через сравнение сырого `select('*')` (работал) с `select('*, agencies(*))` (падал) в консоли браузера. Фикс: явный FK — `agencies!profiles_agency_id_fkey(*)`. Проверено — вход и «Объекты» работают ✅ |
| Иерархия агентств: superadmin → руководитель → МОП → агент + KPI-дашборд | 2026-07-04 | Модель: superadmin (владелец) создаёт агентство и приглашает руководителя → руководитель приглашает МОПов → МОП приглашает агентов. Открытый self-signup закрыт (RLS), теперь только приглашение по ссылке (`?invite=токен`), приглашённый сам регистрируется и подтверждается через RPC `accept_invite` (сверяет email, привязывает `mop_id` к пригласившему). `supabase/agency_hierarchy.sql`: роль `superadmin`, `agencies.subscription_status`, `properties.exclusivity` (эксклюзив/СНР), `profiles.hired_at`/`deposits_manual`/`volume_manual` для ручного бэкфилла истории агентства. Экран «Мои агентства» (superadmin) — список + создание. Экран «Моя команда» (admin/mop) — KPI (объекты по категориям, новые за 24ч/неделю/месяц, обработано эксклюзив+СНР) + список МОПов→агентов с редактируемыми показателями. Токены/бонусы/Face ID — сознательно отложены, зафиксированы отдельными пунктами. Требует ручного запуска SQL в Supabase Dashboard + назначения себя superadmin (`UPDATE profiles SET role='superadmin' WHERE id=...`). Деплой success ✅ |
| МОП: экран подтверждения переноса объекта | 2026-07-04 | Кнопка «Передать коллеге» в редакторе объекта («База») → список агентов агентства (`Sb.getAgencyAgents`) → заявка (`Sb.requestTransfer`). На Профиле у роли mop/admin — карточка «Заявки на перенос» со счётчиком → список с Подтвердить/Отклонить (`Sb.decideTransfer` → RPC `approve_transfer` из `mop_transfers.sql`, применённой в Supabase). Попутно нашёл и исправил баг предыдущего коммита ребрендинга: `git add -A` был ограничен путями и не подхватил удаление старых `projects/24streets*` — они задвоились в истории; удалены. Проверено: деплой success, `edTransferBtn`/`mopTransfersCard` есть в проде ✅ |
| Кнопка «Передать коллеге» в «Мои объекты» + фикс навигации | 2026-07-04 | Та же кнопка добавлена в `screen-prop-editor` (agent-properties.js), видна только для существующего объекта. Добавлена чипа «Дача» в этот редактор (была только в редакторе «Базы» — расхождение форм). Ревью нашло реальный баг: у `screen-transfers` не было кнопки «назад» (тупик навигации) — убран `with-tabs`, добавлен `transfersBack` по паттерну `screen-lead-detail`. Проверено: деплой success ✅ |
| Ребрендинг 24streets → agnt.24 | 2026-07-04 | Название "24streets" оказалось занято другим продуктом на рынке. Переименовано everywhere: `projects/24streets`→`projects/agnt-24`, `projects/24streets-app`→`projects/agnt-24-app`, workflow `deploy-24streets.yml`→`deploy-agnt-24.yml` (+ пути внутри), все внутренние пути в `pipeline.py`/`build_app_data.py`, тайтлы/README/лого → `agnt.24`. Windows Task Scheduler: задача `24streetsPipeline`→`agnt24Pipeline` с обновлённым путём (иначе автозапуск 07:00 сломался бы). Попутно устранена бомба замедленного действия: осиротевший `.git` внутри `realty-lead-bot` (не был подключён как submodule) чуть не превратился в битую gitlink-ссылку при коммите — удалён локально, репозиторий на GitHub не затронут. Проверено: деплой прошёл автоматически по новым путям (success), все 3 URL живые, `<title>agnt.24</title>` ✅ |
| Только хозяева в парсере + закрепление объектов за агентом/агентством + разрешение МОП на перенос | 2026-07-04 | `das[who]=1` во всех SEARCH_URLS парсера — агентские объявления больше не попадают в базу. Фикс критичного бага удаления фото без подтверждения (и в «Базе», и в «Мои объекты»). «В базу» теперь синхронизируется в Supabase `properties` (agency_id+agent_id) через `_syncClaimToSupabase` — раньше жило только в localStorage. Экран «Мои объекты» сгруппирован по району → категории. `supabase/mop_transfers.sql`: роль 'mop', `transfer_requests`, RPC `approve_transfer()`, закрыта дыра в RLS (агент раньше мог сам сменить `agent_id`). Требует ручного запуска в Supabase Dashboard + экрана UI (см. «МОП: подтверждение переноса» выше) ✅ |
| Деплой realty-catalog и realty-quick-match на GitHub Pages + автопуш listings.js | 2026-07-04 | `deploy-agnt-24.yml` расширен: собирает `_site` из `agnt-24-app` (корень), `realty-catalog` (`/catalog`), `realty-quick-match` (`/quick-match`), триггер на изменения во всех трёх папках + `workflow_dispatch`. `pipeline.py` теперь коммитит и пушит обновлённые `listings.js` после каждого прогона (это и запускает деплой). Проверено: ручной запуск workflow — success, все 3 URL отдают 200 — https://aituanze.github.io/cloud/ , /catalog/ , /quick-match/ ✅ |
| Автозапуск pipeline.py через Windows Task Scheduler ежедневно | 2026-07-03 | Задача `agnt24Pipeline` в Task Scheduler, триггер daily 07:00 (Asia/Almaty, до начала рабочего дня в 8–9 утра), действие — `inbox/run_pipeline_daily.bat` (лог в `inbox/pipeline_log.txt`, гитигнорится). В `pipeline.py` добавлен 4-й шаг — `agnt-24-app/data/build_app_data.py` (раньше пайплайн обновлял только realty-catalog и realty-quick-match, НЕ agnt-24-app!). `enrich_details()` (фото/состояние) сознательно не включён в cron — дорогой по капче, как и было закомментировано в самом парсере, запускать вручную. Проверено: 2 прогона (вручную + через сам Task Scheduler, `LastTaskResult=0`) — 811→1219→1243 объявления, firstSeen теперь реально разный (06-28 старые / 07-03 новые), т.е. тайм-фильтры 24ч/3д/неделя в приложении получат реальные данные с завтрашнего дня ✅ |
| Фикс: тип объекта не влиял на пузыри карты | 2026-07-03 | `renderMap()` теперь фильтрует `distCounts` по `state.type` — клик по Дома/Участки/Коммерч./Дачи меняет числа в пузырях. Проверено Playwright: apt→house counts различаются ✅ |
| Фикс: вкладки 3 дня/Неделя не меняли данные | 2026-07-03 | Общий `App.timeCutoffMs()`; «24 часа» = с 8:00 текущих/предыдущих суток (не скользящее окно). Экран района и фильтра теперь показывают реальное число «новых» за период при переключении вкладок ✅ |
| Постоянное нижнее меню на всех экранах | 2026-07-03 | Карта/База/Объекты/Сделки/Профиль видно на экранах района/фильтра/ленты (раньше пропадало — не было способа быстро вернуться). `switchTab()` закрывает вложенные экраны из любого места. Проверено Playwright: тап «База» из фильтра корректно закрывает район+фильтр, тап «Карта» возвращает на карту ✅ |
| Живое обновление счётчиков (поллинг listings.js) | 2026-07-03 | Раз в 2 мин перечитывается `data/listings.js`; новые объекты с krisha.kz подхватываются без перезагрузки. Проверено: имитация нового объекта в Бостандыкском → пузырь 47→48 ✅ |
| Экран База: МОП-Агент / дата+время / ссылка на источник | 2026-07-03 | Карточка теперь показывает кто закрепил объект, дата И время фиксации (было только дата), кнопка «Источник →» на объявление krisha.kz ✅ |
| Рейтинг агента (Яндекс-Такси-style) + звёзды в ленте | 2026-07-03 | `js/agent-rating.js`: звёзды 3.5–5.0 от дисциплины, уровни Бронза→Серебро→Золото→Платина от объёма + порога рейтинга, привилегии на уровень. Карточка в Профиле (звезда/бейдж/прогресс/привилегии). В ленте у объектов коллег — их рейтинг (★4.9) рядом с именем ✅ |
| Реальные фото объявлений (хотлинк CDN krisha) | 2026-07-03 | По анализу ilvo.pro (импорт по одному объекту, не рынок целиком): `parse_photos()`+`enrich_details()` в парсере тянут до 5 ссылок на фото (280x175, есть у всех фото галереи) с detail-страниц, без скачивания. `build_app_data.py` → `photos` в `listings.js`. Лента: `<img loading="lazy">` вместо градиента. «В базу»: автоимпорт фото с krisha в `editData.photos`, редактируемо агентом. Обогащено 811/811 (785 с фото, 6 сняты с публикации). Проверено в браузере (Playwright): 46/46 карточек с реальными фото, claim-flow подтянул 5 фото в редактор, ошибок нет ✅ |
| Screen 2.5 — фильтр «Состояние» | 2026-07-03 | `condition` из detail-страниц krisha (`flat.renovation`/`house.renewal`) → `enrich_conditions()` в парсере → `build_app_data.py` → фильтр-чипы в `app.js`. Обогащено 811/811 объявлений (76 с заполненным состоянием: 53 черновая, 23 ремонт). Проверено в браузере (Playwright): секция рендерится, клик по чипу сузил список 46→4, ошибок в консоли нет ✅ |
| Фикс: `init_db()` больше не дропает `listings.db` при добавлении колонки | 2026-07-03 | Убрана деструктивная логика пересоздания таблицы при несовпадении схемы — миграции теперь только аддитивные (ALTER TABLE). Инцидент: старая логика случайно стёрла 829 объявлений, восстановлено из `listings.csv` ✅ |
| Создать pipeline.py (мастер-скрипт) | 2026-06-28 | `inbox/pipeline.py` — цепочка парсер → build_data × 2 → Telegram |
| Добавить APScheduler в krisha_parser | 2026-06-28 | `run()` возвращает stats dict, `pipeline.py --cron` запускает по расписанию |
| Создать match_lead.py | 2026-06-28 | `realty-lead-bot/match_lead.py` — автоподбор объектов под текст лида |
| Интегрировать матчинг в bot.py | 2026-06-28 | После нового лида бот присылает агентам топ-5 объектов |
| Добавить LISTINGS_DB_PATH в config | 2026-06-28 | `config.py` + `.env.example` обновлены |
| Создать reporter.py | 2026-06-28 | `inbox/reporter.py` — еженедельный Telegram-отчёт по лидам и объектам |
| Railway deployment files | 2026-06-28 | `Procfile` + `runtime.txt` в `realty-lead-bot/` |
| Тест pipeline.py локально | 2026-06-28 | 811 объявлений спарсено, оба build_data.py прошли ✅ |
| Создать inbox/.env + venv | 2026-06-28 | `.venv` в `inbox/`, все зависимости установлены |
| Деплой бота на Railway | 2026-06-28 | @Aituanzesbot запущен, /start работает, уведомления в группу agnt.24 приходят ✅ |
| Подключить реальные данные к agnt-24-app | 2026-06-30 | build_app_data.py читает 811 объектов из listings.db → 769 активных + 60 архивных (синт.) + добавлен Ауэзовский район ✅ |
| Деплой agnt-24-app на GitHub Pages | 2026-06-30 | https://aituanze.github.io/cloud/ — репо публичное, CI/CD через Actions ✅ |
| agnt.24 App — 4 экрана + навигация | 2026-07-01 | Все 4 экрана готовы, таб-бар работает, реальные данные подключены, деплой активен ✅ |
| Screen 1 — Pulse Map финальная версия | 2026-07-01 | Горы Алатау, 3-тоггл, архив-пунктир, счётчик «сейчас» — всё в коде ✅ |
| Screen 2 — шрифт Inter 700/500 | 2026-07-01 | tc-count: 900→700, tc-label: 600→500 ✅ |
| Screen 2.5 — синие выбранные чипы | 2026-07-01 | `.filt-chip.on { background: var(--blue) }` — уже реализовано ✅ |
| Screen 3 — TikTok-фид подтверждён | 2026-07-01 | Палитра, Inter 700/500-600, тёплый серый — всё соответствует макету ✅ |
| Удалить артефакт NUL | 2026-07-01 | `projects/agnt-24-app/data/NUL` удалён ✅ |
| Фикс кодировки build_app_data.py | 2026-07-01 | `sys.stdout.reconfigure(encoding='utf-8')` добавлен ✅ |
| Layer 2 — дизайн-документ написан | 2026-07-01 | `docs/superpowers/specs/2026-07-01-layer2-crm-design.md` — одна база, два окна, Supabase, RLS, роли ✅ |
| Layer 2 — план имплементации написан | 2026-07-01 | `docs/superpowers/plans/2026-07-01-layer2-crm.md` — 9 задач с полным кодом, без плейсхолдеров ✅ |
| Layer 2 — имплементация завершена | 2026-07-02 | Auth + агентское окно + CRM Kanban + лента покупателя + Supabase ✅ |
