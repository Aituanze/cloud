# Layer 2: CRM + Buyer Window Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить двухоконную платформу (агент + покупатель) поверх существующего 24streets-app с Supabase как бэкендом.

**Architecture:** Один PWA, роль определяется при авторизации. Агент видит Pulse Map + управление объектами + CRM. Покупатель видит TikTok-ленту опубликованных объектов и разблокирует контакт через телефон OTP.

**Tech Stack:** Vanilla JS (без сборщика), Supabase JS v2 (CDN), Supabase Storage для фото, Supabase Auth (email для агентов, phone OTP для покупателей), GitHub Pages.

## Global Constraints

- Никаких npm/bundler — только CDN и нативный JS ES6
- Файл `supabase-config.js` с env-переменными НЕ коммитить (добавить в .gitignore)
- Все новые экраны — класс `.screen` + `id="screen-*"` (как существующие)
- CSS-переменные: `--ink:#1a1831`, `--blue:#2d6be4`, `--green:#15966b`, `--border:#e8e5de`
- Тёплый серый (`#f2f0eb` / `--bg`) фон новых экранов (как Screen 3)
- Шрифт: Inter 700 для цифр, Inter 500-600 для текста
- Supabase project URL и anon key — из переменных в `js/supabase-config.js`
- RLS включён на всех таблицах с самого начала

---

## Файловая структура

```
projects/24streets-app/
├── index.html                    ← MODIFY: добавить новые screen-секции + script теги
├── css/
│   ├── styles.css                ← MODIFY: добавить стили новых экранов
├── js/
│   ├── app.js                    ← MODIFY: добавить init auth + role routing
│   ├── supabase-config.js        ← CREATE (в .gitignore): SUPABASE_URL, SUPABASE_KEY
│   ├── supabase-client.js        ← CREATE: init клиент, auth helpers, db helpers
│   ├── auth.js                   ← CREATE: экраны входа (агент email, покупатель phone)
│   ├── agent-properties.js       ← CREATE: управление объектами агента
│   ├── agent-crm.js              ← CREATE: CRM-воронка Kanban
│   └── buyer-feed.js             ← CREATE: лента покупателя + contact gate
└── data/
    └── listings.js               ← без изменений (статика остаётся как fallback)

supabase/ (в корне projects/24streets/)
├── schema.sql                    ← CREATE: полная схема БД
└── rls.sql                       ← CREATE: RLS-политики
```

---

## Task 1: Supabase проект + схема БД

**Files:**
- Create: `projects/24streets/supabase/schema.sql`
- Create: `projects/24streets/supabase/rls.sql`

**Interfaces:**
- Produces: таблицы `agencies`, `profiles`, `invites`, `properties`, `buyer_profiles`, `leads`, `lead_events` в Supabase

- [ ] **Step 1: Создать Supabase проект**

  Перейди на https://supabase.com → New Project → имя: `24streets` → регион: ближайший (например Frankfurt) → пароль БД (сохрани).

- [ ] **Step 2: Создать файл schema.sql**

```sql
-- projects/24streets/supabase/schema.sql

CREATE TABLE agencies (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  name text NOT NULL,
  city text DEFAULT 'Алматы',
  logo_url text,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE profiles (
  id uuid REFERENCES auth.users ON DELETE CASCADE PRIMARY KEY,
  agency_id uuid REFERENCES agencies,
  role text NOT NULL CHECK (role IN ('admin', 'agent')),
  name text NOT NULL,
  phone text,
  avatar_url text,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE invites (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  agency_id uuid REFERENCES agencies NOT NULL,
  email text,
  role text DEFAULT 'agent' CHECK (role IN ('admin', 'agent')),
  status text DEFAULT 'pending' CHECK (status IN ('pending', 'accepted', 'expired')),
  invited_by uuid REFERENCES profiles,
  token text UNIQUE DEFAULT gen_random_uuid()::text,
  expires_at timestamptz DEFAULT now() + interval '7 days',
  created_at timestamptz DEFAULT now()
);

CREATE TABLE properties (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  agency_id uuid REFERENCES agencies NOT NULL,
  agent_id uuid REFERENCES profiles NOT NULL,
  source_krisha_id text,
  type text CHECK (type IN ('apt', 'house', 'land', 'commercial')),
  district text,
  address text,
  price bigint,
  price_label text,
  area numeric,
  rooms integer,
  floor integer,
  floors integer,
  building_type text,
  description text,
  owner_name text,
  owner_phone text,
  photos text[],
  status text DEFAULT 'draft' CHECK (status IN ('draft', 'active', 'archived')),
  published_at timestamptz,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);

CREATE TABLE buyer_profiles (
  id uuid REFERENCES auth.users ON DELETE CASCADE PRIMARY KEY,
  phone text UNIQUE NOT NULL,
  name text,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE leads (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  property_id uuid REFERENCES properties NOT NULL,
  agent_id uuid REFERENCES profiles NOT NULL,
  buyer_id uuid REFERENCES buyer_profiles,
  buyer_phone text,
  stage text DEFAULT 'new'
    CHECK (stage IN ('new','contacted','showing','deposit','deal','lost')),
  notes text,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);

CREATE TABLE lead_events (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  lead_id uuid REFERENCES leads NOT NULL,
  stage_from text,
  stage_to text NOT NULL,
  note text,
  created_by uuid REFERENCES profiles,
  created_at timestamptz DEFAULT now()
);

-- Storage bucket для фото
INSERT INTO storage.buckets (id, name, public) VALUES ('property-photos', 'property-photos', true);
```

- [ ] **Step 3: Создать файл rls.sql**

```sql
-- projects/24streets/supabase/rls.sql

ALTER TABLE agencies ENABLE ROW LEVEL SECURITY;
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE invites ENABLE ROW LEVEL SECURITY;
ALTER TABLE properties ENABLE ROW LEVEL SECURITY;
ALTER TABLE buyer_profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE leads ENABLE ROW LEVEL SECURITY;
ALTER TABLE lead_events ENABLE ROW LEVEL SECURITY;

-- Agencies: только участники агентства
CREATE POLICY "Agency members read" ON agencies FOR SELECT
  USING (id IN (SELECT agency_id FROM profiles WHERE id = auth.uid()));

-- Profiles: своё агентство
CREATE POLICY "Same agency profiles read" ON profiles FOR SELECT
  USING (agency_id IN (SELECT agency_id FROM profiles WHERE id = auth.uid()));
CREATE POLICY "Own profile insert" ON profiles FOR INSERT
  WITH CHECK (id = auth.uid());
CREATE POLICY "Own profile update" ON profiles FOR UPDATE
  USING (id = auth.uid());

-- Properties: active — всем; полный доступ — своему агентству
CREATE POLICY "Public read active" ON properties FOR SELECT
  USING (status = 'active');
CREATE POLICY "Agent read own agency" ON properties FOR SELECT
  USING (agency_id IN (SELECT agency_id FROM profiles WHERE id = auth.uid()));
CREATE POLICY "Agent insert" ON properties FOR INSERT
  WITH CHECK (agent_id = auth.uid());
CREATE POLICY "Agent update own" ON properties FOR UPDATE
  USING (agent_id = auth.uid());

-- Buyer profiles: только своя запись
CREATE POLICY "Own buyer profile" ON buyer_profiles FOR ALL
  USING (id = auth.uid());

-- Leads: только свои агенты
CREATE POLICY "Agent own leads" ON leads FOR ALL
  USING (agent_id = auth.uid());
CREATE POLICY "System insert lead" ON leads FOR INSERT
  WITH CHECK (true);

-- Lead events: через leads
CREATE POLICY "Agent lead events" ON lead_events FOR SELECT
  USING (lead_id IN (SELECT id FROM leads WHERE agent_id = auth.uid()));
CREATE POLICY "Agent insert lead event" ON lead_events FOR INSERT
  WITH CHECK (true);

-- Storage: чтение всем, запись только авторизованным агентам
CREATE POLICY "Public photo read" ON storage.objects FOR SELECT
  USING (bucket_id = 'property-photos');
CREATE POLICY "Agent photo upload" ON storage.objects FOR INSERT
  WITH CHECK (bucket_id = 'property-photos' AND auth.uid() IS NOT NULL);
CREATE POLICY "Agent photo delete" ON storage.objects FOR DELETE
  USING (bucket_id = 'property-photos' AND auth.uid() IS NOT NULL);
```

- [ ] **Step 4: Выполнить SQL в Supabase**

  Dashboard → SQL Editor → New Query → вставь `schema.sql` → Run.
  Затем вставь `rls.sql` → Run.

- [ ] **Step 5: Проверить**

  Dashboard → Table Editor → убедись что все 7 таблиц созданы.
  Storage → убедись что bucket `property-photos` существует.

- [ ] **Step 6: Получить credentials**

  Dashboard → Project Settings → API → скопируй:
  - Project URL: `https://xxxx.supabase.co`
  - anon public key: `eyJ...`

- [ ] **Step 7: Commit**

```bash
git add projects/24streets/supabase/schema.sql projects/24streets/supabase/rls.sql
git commit -m "feat: add supabase schema and rls policies"
```

---

## Task 2: Supabase клиент в app

**Files:**
- Create: `projects/24streets-app/js/supabase-config.js` (в .gitignore)
- Create: `projects/24streets-app/js/supabase-client.js`
- Modify: `projects/24streets-app/index.html` (добавить script теги)
- Modify: `projects/24streets-app/.gitignore` (создать если нет)

**Interfaces:**
- Produces: глобальный объект `Sb` с методами: `Sb.auth`, `Sb.getProfile()`, `Sb.getSession()`

- [ ] **Step 1: Добавить .gitignore**

```
# projects/24streets-app/.gitignore
js/supabase-config.js
```

- [ ] **Step 2: Создать supabase-config.js**

```js
// js/supabase-config.js — НЕ КОММИТИТЬ
const SUPABASE_URL  = 'https://ТВОЙ_PROJECT_ID.supabase.co';
const SUPABASE_KEY  = 'ТВОЙ_ANON_KEY';
```

- [ ] **Step 3: Создать supabase-client.js**

```js
// js/supabase-client.js
const { createClient } = supabase;
const _db = createClient(SUPABASE_URL, SUPABASE_KEY);

const Sb = {
  auth: _db.auth,
  db:   _db,

  async getSession() {
    const { data: { session } } = await _db.auth.getSession();
    return session;
  },

  async getProfile(uid) {
    const { data } = await _db
      .from('profiles')
      .select('*, agencies(*)')
      .eq('id', uid)
      .single();
    return data;
  },

  async getBuyerProfile(uid) {
    const { data } = await _db
      .from('buyer_profiles')
      .select('*')
      .eq('id', uid)
      .single();
    return data;
  },

  async getPublishedProperties(limit = 50) {
    const { data } = await _db
      .from('properties')
      .select('id,type,district,address,price,price_label,area,rooms,floor,floors,building_type,photos,description,published_at,agent_id')
      .eq('status', 'active')
      .order('published_at', { ascending: false })
      .limit(limit);
    return data || [];
  },

  async getAgentProperties(agentId) {
    const { data } = await _db
      .from('properties')
      .select('*')
      .eq('agent_id', agentId)
      .order('created_at', { ascending: false });
    return data || [];
  },

  async upsertProperty(obj) {
    const { data, error } = await _db
      .from('properties')
      .upsert(obj)
      .select()
      .single();
    if (error) throw error;
    return data;
  },

  async uploadPhoto(file, propertyId) {
    const ext  = file.name.split('.').pop();
    const path = `${propertyId}/${Date.now()}.${ext}`;
    const { error } = await _db.storage
      .from('property-photos')
      .upload(path, file, { upsert: true });
    if (error) throw error;
    const { data: { publicUrl } } = _db.storage
      .from('property-photos')
      .getPublicUrl(path);
    return publicUrl;
  },

  async createLead(propertyId, agentId, buyerId, buyerPhone) {
    const { data, error } = await _db
      .from('leads')
      .insert({ property_id: propertyId, agent_id: agentId, buyer_id: buyerId, buyer_phone: buyerPhone, stage: 'new' })
      .select()
      .single();
    if (error) throw error;
    return data;
  },

  async getAgentLeads(agentId) {
    const { data } = await _db
      .from('leads')
      .select('*, properties(address,price_label,photos), buyer_profiles(phone,name)')
      .eq('agent_id', agentId)
      .order('created_at', { ascending: false });
    return data || [];
  },

  async updateLeadStage(leadId, stageTo, note) {
    const lead = await _db.from('leads').select('stage').eq('id', leadId).single();
    await _db.from('leads').update({ stage: stageTo, updated_at: new Date().toISOString() }).eq('id', leadId);
    await _db.from('lead_events').insert({
      lead_id: leadId,
      stage_from: lead.data?.stage,
      stage_to: stageTo,
      note: note || null,
    });
  },
};
```

- [ ] **Step 4: Добавить script теги в index.html перед закрывающим </body>**

Найди строку где подключается `app.js` и добавь ПЕРЕД ней:

```html
<!-- Supabase -->
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.js"></script>
<script src="js/supabase-config.js"></script>
<script src="js/supabase-client.js"></script>
<!-- Auth + роли -->
<script src="js/auth.js"></script>
<!-- Агент -->
<script src="js/agent-properties.js"></script>
<script src="js/agent-crm.js"></script>
<!-- Покупатель -->
<script src="js/buyer-feed.js"></script>
```

- [ ] **Step 5: Проверить в консоли браузера**

Открой app. В DevTools Console:
```js
Sb.getSession().then(s => console.log('session:', s))
```
Ожидаемый результат: `session: null` (не залогинен).

- [ ] **Step 6: Commit**

```bash
git add projects/24streets-app/js/supabase-client.js projects/24streets-app/index.html projects/24streets-app/.gitignore
git commit -m "feat: add supabase client and auth helpers"
```

---

## Task 3: Экраны авторизации

**Files:**
- Create: `projects/24streets-app/js/auth.js`
- Modify: `projects/24streets-app/index.html` (добавить screen-auth + screen-phone-otp)
- Modify: `projects/24streets-app/css/styles.css` (добавить стили auth)

**Interfaces:**
- Produces: функции `Auth.showAgentLogin()`, `Auth.showPhoneOTP(onSuccess)`, `Auth.signOut()`
- Consumes: `Sb.auth`, `Sb.getProfile()`

- [ ] **Step 1: Добавить auth-экраны в index.html**

Добавь перед закрывающим `</div>` (перед `</div id="app">`):

```html
<!-- ══════ SCREEN AUTH (агент) ══════ -->
<div id="screen-auth" class="screen slide-below auth-screen">
  <div class="auth-logo">24<span>Streets</span></div>
  <div class="auth-tagline">Платформа для риэлторов Алматы</div>
  <div class="auth-card">
    <div class="auth-title">Вход для агентов</div>
    <div id="authError" class="auth-error" style="display:none"></div>
    <input id="authEmail" class="auth-input" type="email" placeholder="Email" autocomplete="email">
    <input id="authPassword" class="auth-input" type="password" placeholder="Пароль" autocomplete="current-password">
    <button id="authSubmit" class="auth-btn">Войти</button>
    <div class="auth-divider">Покупаете квартиру?</div>
    <button id="authBuyerBtn" class="auth-btn-secondary">Смотреть объекты →</button>
  </div>
</div>

<!-- ══════ SCREEN PHONE OTP (покупатель) ══════ -->
<div id="screen-phone-otp" class="screen slide-below auth-screen">
  <div class="auth-logo">24<span>Streets</span></div>
  <div class="auth-card">
    <div class="auth-title" id="otpTitle">Введите номер телефона</div>
    <div class="auth-sub" id="otpSub">Чтобы увидеть контакт агента</div>
    <div id="otpError" class="auth-error" style="display:none"></div>
    <div id="otpPhoneStep">
      <input id="otpPhone" class="auth-input" type="tel" placeholder="+7 777 000 00 00" autocomplete="tel">
      <button id="otpSendBtn" class="auth-btn">Получить код</button>
      <button id="otpSkipBtn" class="auth-btn-secondary" style="margin-top:8px">← Назад к ленте</button>
    </div>
    <div id="otpCodeStep" style="display:none">
      <input id="otpCode" class="auth-input" type="number" placeholder="Код из SMS" maxlength="6">
      <button id="otpVerifyBtn" class="auth-btn">Подтвердить</button>
    </div>
  </div>
</div>
```

- [ ] **Step 2: Добавить стили в styles.css**

```css
/* ══════════════════════════════════════════
   AUTH SCREENS
══════════════════════════════════════════ */
.auth-screen {
  background: var(--bg);
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  padding: 24px 20px calc(var(--safe-bot) + 24px);
}
.auth-logo {
  font-size: 28px; font-weight: 900; color: var(--ink);
  letter-spacing: -0.5px; margin-bottom: 4px;
}
.auth-logo span { color: var(--blue); }
.auth-tagline {
  font-size: 12px; color: var(--ink2); font-weight: 500;
  margin-bottom: 32px;
}
.auth-card {
  width: 100%; max-width: 360px;
  background: white; border-radius: 20px;
  padding: 24px 20px; box-shadow: 0 4px 24px rgba(0,0,0,.08);
}
.auth-title {
  font-size: 18px; font-weight: 700; color: var(--ink);
  margin-bottom: 20px;
}
.auth-sub {
  font-size: 13px; color: var(--ink2); margin-bottom: 16px;
}
.auth-input {
  width: 100%; height: 46px; border-radius: 12px;
  border: 1.5px solid var(--border); background: var(--s2);
  font-size: 15px; color: var(--ink); padding: 0 14px;
  font-family: 'Inter', sans-serif; outline: none;
  margin-bottom: 12px; display: block;
}
.auth-input:focus { border-color: var(--blue); }
.auth-btn {
  width: 100%; height: 46px; border-radius: 12px;
  background: var(--ink); color: white;
  font-size: 14px; font-weight: 700;
  border: none; cursor: pointer;
  font-family: 'Inter', sans-serif;
  transition: opacity .15s;
}
.auth-btn:active { opacity: .85; }
.auth-btn-secondary {
  width: 100%; height: 40px; border-radius: 12px;
  background: transparent; color: var(--ink2);
  font-size: 13px; font-weight: 600;
  border: 1.5px solid var(--border); cursor: pointer;
  font-family: 'Inter', sans-serif; margin-top: 10px;
}
.auth-divider {
  text-align: center; font-size: 12px; color: var(--ink3);
  margin: 16px 0 10px;
}
.auth-error {
  background: rgba(220,68,70,.1); color: #dc4446;
  border-radius: 10px; padding: 10px 14px;
  font-size: 12px; font-weight: 600; margin-bottom: 12px;
}
```

- [ ] **Step 3: Создать auth.js**

```js
// js/auth.js
const Auth = {
  _otpSuccessCb: null,

  showAgentLogin() {
    document.querySelectorAll('.screen').forEach(s => {
      s.classList.remove('active');
      s.classList.add('slide-below');
    });
    const s = document.getElementById('screen-auth');
    s.classList.remove('slide-below');
    s.classList.add('active');
    document.getElementById('tabBar')?.classList.add('hidden');
  },

  showPhoneOTP(onSuccess) {
    this._otpSuccessCb = onSuccess;
    document.getElementById('otpPhoneStep').style.display = '';
    document.getElementById('otpCodeStep').style.display = 'none';
    document.getElementById('otpError').style.display = 'none';
    document.getElementById('otpPhone').value = '';
    document.getElementById('otpCode').value = '';
    const s = document.getElementById('screen-phone-otp');
    s.classList.remove('slide-below');
    s.classList.add('active');
  },

  hidePhoneOTP() {
    const s = document.getElementById('screen-phone-otp');
    s.classList.add('slide-below');
    s.classList.remove('active');
  },

  async signOut() {
    await Sb.auth.signOut();
    location.reload();
  },

  _showError(elId, msg) {
    const el = document.getElementById(elId);
    el.textContent = msg;
    el.style.display = 'block';
  },

  bind() {
    // Агент: вход по email
    document.getElementById('authSubmit').addEventListener('click', async () => {
      const email = document.getElementById('authEmail').value.trim();
      const pass  = document.getElementById('authPassword').value;
      document.getElementById('authError').style.display = 'none';
      if (!email || !pass) {
        this._showError('authError', 'Введите email и пароль'); return;
      }
      const { error } = await Sb.auth.signInWithPassword({ email, password: pass });
      if (error) { this._showError('authError', 'Неверный email или пароль'); return; }
      location.reload();
    });

    // Покупатель: перейти к ленте анонимно
    document.getElementById('authBuyerBtn').addEventListener('click', () => {
      BuyerFeed.show();
    });

    // OTP: отправить код
    document.getElementById('otpSendBtn').addEventListener('click', async () => {
      let phone = document.getElementById('otpPhone').value.trim();
      if (!phone.startsWith('+')) phone = '+7' + phone.replace(/\D/g,'').slice(-10);
      document.getElementById('otpError').style.display = 'none';
      const { error } = await Sb.auth.signInWithOtp({ phone });
      if (error) { this._showError('otpError', 'Не удалось отправить SMS'); return; }
      document.getElementById('otpPhoneStep').style.display = 'none';
      document.getElementById('otpCodeStep').style.display = '';
      document.getElementById('otpTitle').textContent = 'Введите код из SMS';
      document.getElementById('otpSub').textContent = `Код отправлен на ${phone}`;
    });

    // OTP: подтвердить код
    document.getElementById('otpVerifyBtn').addEventListener('click', async () => {
      const phone = document.getElementById('otpPhone').value.trim();
      const token = document.getElementById('otpCode').value.trim();
      const { error, data } = await Sb.auth.verifyOtp({ phone: phone.startsWith('+') ? phone : '+7'+phone.replace(/\D/g,'').slice(-10), token, type: 'sms' });
      if (error) { this._showError('otpError', 'Неверный код'); return; }
      // Сохранить buyer_profile
      await Sb.db.from('buyer_profiles')
        .upsert({ id: data.user.id, phone: data.user.phone })
        .select();
      this.hidePhoneOTP();
      if (this._otpSuccessCb) this._otpSuccessCb(data.user);
    });

    // OTP: назад
    document.getElementById('otpSkipBtn').addEventListener('click', () => {
      this.hidePhoneOTP();
    });
  },
};
```

- [ ] **Step 4: Проверить**

  Открой app. Должен видеть пустую страницу (app.js ещё не вызывает Auth).
  В консоли нет ошибок JS.

- [ ] **Step 5: Commit**

```bash
git add projects/24streets-app/js/auth.js projects/24streets-app/index.html projects/24streets-app/css/styles.css
git commit -m "feat: add auth screens for agents and buyers"
```

---

## Task 4: Role routing — два окна

**Files:**
- Modify: `projects/24streets-app/js/app.js` (заменить `App.init()`)

**Interfaces:**
- Consumes: `Sb.getSession()`, `Sb.getProfile()`, `Sb.getBuyerProfile()`
- Produces: вызывает `Auth.showAgentLogin()` / `AgentApp.init()` / `BuyerFeed.show()`

- [ ] **Step 1: Заменить вызов init в app.js**

В самом конце `app.js` найди строку:
```js
document.addEventListener('DOMContentLoaded', () => App.init());
```
Замени на:

```js
document.addEventListener('DOMContentLoaded', async () => {
  Auth.bind();

  const session = await Sb.getSession();

  if (!session) {
    // Нет сессии — показать экран агентского логина
    // Покупатель может нажать "Смотреть объекты" и попасть в BuyerFeed
    Auth.showAgentLogin();
    return;
  }

  // Определяем роль
  const agentProfile = await Sb.getProfile(session.user.id);

  if (agentProfile) {
    // Агент или Admin
    window._agentProfile = agentProfile;
    App.init(); // существующий агентский app
    AgentProperties.init(agentProfile);
    AgentCrm.init(agentProfile);
  } else {
    // Покупатель (есть сессия но нет agent profile)
    const buyerProfile = await Sb.getBuyerProfile(session.user.id);
    window._buyerProfile = buyerProfile;
    BuyerFeed.show();
  }
});
```

- [ ] **Step 2: Проверить агентский путь**

  Залогинься через Supabase Dashboard → Authentication → создай test-агента (email + пароль).
  Вручную добавь в таблицу `profiles` запись для этого user id.
  Войди через форму логина → должен загрузиться текущий App (Pulse Map).

- [ ] **Step 3: Проверить покупательский путь**

  Нажми "Смотреть объекты" на экране логина → должна открыться пустая лента (BuyerFeed.show() — реализуем в Task 6).

- [ ] **Step 4: Commit**

```bash
git add projects/24streets-app/js/app.js
git commit -m "feat: role-based routing — agent vs buyer window"
```

---

## Task 5: Экран управления объектами агента

**Files:**
- Create: `projects/24streets-app/js/agent-properties.js`
- Modify: `projects/24streets-app/index.html` (screen-properties)
- Modify: `projects/24streets-app/css/styles.css` (стили)
- Modify: `projects/24streets-app/index.html` (таб-бар агента — добавить вкладку "Объекты")

**Interfaces:**
- Consumes: `Sb.getAgentProperties()`, `Sb.upsertProperty()`, `Sb.uploadPhoto()`, `window._agentProfile`
- Produces: `AgentProperties.init(profile)`, `AgentProperties.openEditor(listing?)`

- [ ] **Step 1: Добавить screen-properties в index.html**

```html
<!-- ══════ SCREEN: МОИ ОБЪЕКТЫ (агент) ══════ -->
<div id="screen-properties" class="screen slide-below">
  <!-- Header -->
  <div class="ds-status-bar"></div>
  <div class="ds-header" style="background:var(--s2);border-bottom:1px solid var(--border);">
    <div style="flex:1;font-size:16px;font-weight:700;color:var(--ink)">Мои объекты</div>
    <button id="propAddBtn" class="prop-add-btn">+ Добавить</button>
  </div>
  <!-- List -->
  <div id="propList" class="prop-list"></div>
</div>

<!-- ══════ SCREEN: РЕДАКТОР ОБЪЕКТА ══════ -->
<div id="screen-prop-editor" class="screen slide-below">
  <div class="ds-status-bar"></div>
  <div class="ds-header" style="background:var(--s2);border-bottom:1px solid var(--border);">
    <div id="propEditorBack" class="ds-back">
      <svg width="7" height="12" viewBox="0 0 7 12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><polyline points="6,1 1,6 6,11"/></svg>
    </div>
    <div id="propEditorTitle" style="flex:1;font-size:16px;font-weight:700;color:var(--ink);margin-left:12px">Новый объект</div>
    <button id="propPublishBtn" class="prop-publish-btn">Опубликовать</button>
  </div>
  <div class="prop-editor-scroll">
    <!-- Фото -->
    <div class="pe-section-label">Фото (до 10)</div>
    <div id="pePhotosRow" class="pe-photos-row">
      <label class="pe-photo-add">
        <input id="pePhotoInput" type="file" accept="image/*" multiple style="display:none">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="var(--ink3)" stroke-width="2"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
      </label>
    </div>
    <!-- Тип -->
    <div class="pe-section-label">Тип объекта</div>
    <div class="pe-chips-row" id="peTypeChips">
      <div class="pe-chip on" data-val="apt">Квартира</div>
      <div class="pe-chip" data-val="house">Дом</div>
      <div class="pe-chip" data-val="land">Участок</div>
      <div class="pe-chip" data-val="commercial">Коммерческая</div>
    </div>
    <!-- Поля -->
    <div class="pe-section-label">Адрес</div>
    <input id="peAddress" class="pe-input" placeholder="пр. Достык, 180" type="text">
    <div class="pe-row">
      <div class="pe-field">
        <div class="pe-section-label">Цена (₸)</div>
        <input id="pePrice" class="pe-input" placeholder="45 000 000" type="number">
      </div>
      <div class="pe-field">
        <div class="pe-section-label">Площадь (м²)</div>
        <input id="peArea" class="pe-input" placeholder="65" type="number">
      </div>
    </div>
    <div class="pe-row">
      <div class="pe-field">
        <div class="pe-section-label">Комнат</div>
        <input id="peRooms" class="pe-input" placeholder="2" type="number">
      </div>
      <div class="pe-field">
        <div class="pe-section-label">Этаж / Всего</div>
        <input id="peFloor" class="pe-input" placeholder="4 / 9" type="text">
      </div>
    </div>
    <div class="pe-section-label">Описание</div>
    <textarea id="peDesc" class="pe-input" rows="3" placeholder="Описание объекта..."></textarea>
    <!-- Собственник -->
    <div class="pe-section-label" style="margin-top:16px">Собственник (только агент видит)</div>
    <input id="peOwnerName" class="pe-input" placeholder="Имя собственника" type="text">
    <input id="peOwnerPhone" class="pe-input" placeholder="+7 777 000 00 00" type="tel">
  </div>
</div>
```

- [ ] **Step 2: Добавить стили в styles.css**

```css
/* ══════════════════════════════════════════
   AGENT PROPERTIES SCREEN
══════════════════════════════════════════ */
.prop-add-btn {
  padding: 8px 16px; border-radius: 10px;
  background: var(--blue); color: white;
  font-size: 13px; font-weight: 700; border: none; cursor: pointer;
  font-family: 'Inter', sans-serif;
}
.prop-list {
  flex: 1; overflow-y: auto; padding: 12px;
}
.prop-card {
  background: white; border-radius: 14px;
  border: 1px solid var(--border); margin-bottom: 10px;
  padding: 14px; display: flex; gap: 12px; cursor: pointer;
}
.prop-card-thumb {
  width: 64px; height: 64px; border-radius: 10px;
  object-fit: cover; flex-shrink: 0; background: var(--bg);
}
.prop-card-body { flex: 1; min-width: 0; }
.prop-card-price { font-size: 16px; font-weight: 700; color: var(--ink); }
.prop-card-addr { font-size: 12px; color: var(--ink2); margin-top: 2px;
  white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.prop-card-status {
  display: inline-block; padding: 2px 8px; border-radius: 6px;
  font-size: 10px; font-weight: 700; margin-top: 6px;
}
.prop-card-status.draft   { background: #f5f0eb; color: var(--ink3); }
.prop-card-status.active  { background: rgba(21,150,107,.12); color: var(--green); }
.prop-card-status.archived{ background: rgba(220,68,70,.1); color: #dc4446; }
.prop-publish-btn {
  padding: 8px 14px; border-radius: 10px;
  background: var(--green); color: white;
  font-size: 13px; font-weight: 700; border: none; cursor: pointer;
  font-family: 'Inter', sans-serif;
}

/* Property Editor */
.prop-editor-scroll {
  flex: 1; overflow-y: auto; padding: 16px;
}
.pe-section-label {
  font-size: 10px; font-weight: 700; letter-spacing: .08em;
  color: var(--ink3); text-transform: uppercase; margin: 12px 0 6px;
}
.pe-input {
  width: 100%; border-radius: 12px; border: 1.5px solid var(--border);
  background: var(--s2); padding: 12px 14px;
  font-size: 14px; font-weight: 500; color: var(--ink);
  font-family: 'Inter', sans-serif; outline: none;
  margin-bottom: 8px; display: block; resize: none;
}
.pe-input:focus { border-color: var(--blue); }
.pe-row { display: flex; gap: 10px; }
.pe-field { flex: 1; }
.pe-chips-row { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 8px; }
.pe-chip {
  padding: 8px 14px; border-radius: 10px;
  border: 1.5px solid var(--border); background: white;
  font-size: 13px; font-weight: 600; color: var(--ink2);
  cursor: pointer;
}
.pe-chip.on { border-color: var(--blue); background: #edf3fc; color: var(--blue); }
.pe-photos-row {
  display: flex; gap: 8px; overflow-x: auto; padding-bottom: 4px;
}
.pe-photo-add {
  width: 80px; height: 80px; border-radius: 12px;
  border: 1.5px dashed var(--border); background: var(--s2);
  display: flex; align-items: center; justify-content: center;
  cursor: pointer; flex-shrink: 0;
}
.pe-photo-thumb {
  width: 80px; height: 80px; border-radius: 12px;
  object-fit: cover; flex-shrink: 0; position: relative;
}
```

- [ ] **Step 3: Создать agent-properties.js**

```js
// js/agent-properties.js
const AgentProperties = {
  _profile: null,
  _editingProp: null,
  _photos: [],       // { url, file? } — существующие URL или новые File
  _uploadedUrls: [], // финальные URL после сохранения

  init(profile) {
    this._profile = profile;
    document.getElementById('propAddBtn')
      .addEventListener('click', () => this.openEditor(null));
    document.getElementById('propEditorBack')
      .addEventListener('click', () => slideBack('screen-prop-editor', 'screen-properties'));
    document.getElementById('propPublishBtn')
      .addEventListener('click', () => this._save(true));
    document.getElementById('pePhotoInput')
      .addEventListener('change', e => this._onPhotoPick(e));
    document.querySelectorAll('.pe-chip').forEach(c =>
      c.addEventListener('click', () => {
        document.querySelectorAll('.pe-chip').forEach(x => x.classList.remove('on'));
        c.classList.add('on');
      }));
  },

  async show() {
    const props = await Sb.getAgentProperties(this._profile.id);
    const list  = document.getElementById('propList');
    if (!props.length) {
      list.innerHTML = '<div style="text-align:center;padding:40px 20px;color:var(--ink3);font-size:14px">Нет объектов.<br>Нажмите + Добавить</div>';
    } else {
      list.innerHTML = props.map(p => {
        const thumb = p.photos?.[0]
          ? `<img class="prop-card-thumb" src="${p.photos[0]}">`
          : `<div class="prop-card-thumb"></div>`;
        const statusLabel = { draft:'Черновик', active:'Опубликован', archived:'Архив' }[p.status];
        return `<div class="prop-card" data-id="${p.id}">
          ${thumb}
          <div class="prop-card-body">
            <div class="prop-card-price">${p.price_label || ''} ₸</div>
            <div class="prop-card-addr">${p.address || '—'}</div>
            <div class="prop-card-status ${p.status}">${statusLabel}</div>
          </div>
        </div>`;
      }).join('');
      list.querySelectorAll('.prop-card').forEach(card => {
        card.addEventListener('click', () => {
          const prop = props.find(p => p.id === card.dataset.id);
          if (prop) this.openEditor(prop);
        });
      });
    }
    slideForward('screen-map', 'screen-properties');
    document.getElementById('tabBar').classList.add('hidden');
  },

  openEditor(prop) {
    this._editingProp = prop;
    this._photos = (prop?.photos || []).map(url => ({ url }));
    document.getElementById('propEditorTitle').textContent = prop ? 'Редактировать' : 'Новый объект';
    document.getElementById('peAddress').value  = prop?.address || '';
    document.getElementById('pePrice').value    = prop?.price || '';
    document.getElementById('peArea').value     = prop?.area || '';
    document.getElementById('peRooms').value    = prop?.rooms ?? '';
    document.getElementById('peFloor').value    = prop?.floor && prop?.floors ? `${prop.floor} / ${prop.floors}` : '';
    document.getElementById('peDesc').value     = prop?.description || '';
    document.getElementById('peOwnerName').value  = prop?.owner_name || '';
    document.getElementById('peOwnerPhone').value = prop?.owner_phone || '';
    // Тип
    document.querySelectorAll('.pe-chip').forEach(c => c.classList.toggle('on', c.dataset.val === (prop?.type || 'apt')));
    this._renderPhotos();
    slideForward('screen-properties', 'screen-prop-editor');
  },

  _renderPhotos() {
    const row = document.getElementById('pePhotosRow');
    const addBtn = row.querySelector('.pe-photo-add');
    // удалить старые превью
    row.querySelectorAll('.pe-photo-thumb').forEach(el => el.remove());
    this._photos.forEach((p, i) => {
      const img = document.createElement('img');
      img.className = 'pe-photo-thumb';
      img.src = p.url;
      img.addEventListener('click', () => {
        this._photos.splice(i, 1);
        this._renderPhotos();
      });
      row.insertBefore(img, addBtn);
    });
  },

  _onPhotoPick(e) {
    const files = Array.from(e.target.files).slice(0, 10 - this._photos.length);
    files.forEach(file => {
      const url = URL.createObjectURL(file);
      this._photos.push({ url, file });
    });
    this._renderPhotos();
    e.target.value = '';
  },

  async _save(publish) {
    const btn = document.getElementById('propPublishBtn');
    btn.textContent = 'Сохранение...'; btn.disabled = true;

    try {
      // Загрузить новые фото
      const propId = this._editingProp?.id || crypto.randomUUID();
      const finalUrls = [];
      for (const p of this._photos) {
        if (p.file) {
          const url = await Sb.uploadPhoto(p.file, propId);
          finalUrls.push(url);
        } else {
          finalUrls.push(p.url);
        }
      }

      const floorParts = document.getElementById('peFloor').value.split('/').map(s => parseInt(s.trim()));
      const type = document.querySelector('.pe-chip.on')?.dataset.val || 'apt';
      const price = parseInt(document.getElementById('pePrice').value) || 0;

      const payload = {
        id: propId,
        agency_id: this._profile.agency_id,
        agent_id:  this._profile.id,
        type,
        address:   document.getElementById('peAddress').value.trim(),
        price,
        price_label: price >= 1000000 ? `${Math.round(price/1000000)} млн` : String(price),
        area:   parseFloat(document.getElementById('peArea').value) || null,
        rooms:  parseInt(document.getElementById('peRooms').value) || null,
        floor:  floorParts[0] || null,
        floors: floorParts[1] || null,
        description: document.getElementById('peDesc').value.trim(),
        owner_name:  document.getElementById('peOwnerName').value.trim(),
        owner_phone: document.getElementById('peOwnerPhone').value.trim(),
        photos: finalUrls,
        status: publish ? 'active' : 'draft',
        published_at: publish ? new Date().toISOString() : null,
        updated_at: new Date().toISOString(),
      };

      await Sb.upsertProperty(payload);
      slideBack('screen-prop-editor', 'screen-properties');
      await this.show();
    } finally {
      btn.textContent = 'Опубликовать'; btn.disabled = false;
    }
  },
};
```

- [ ] **Step 4: Добавить вкладку "Объекты" в таб-бар агента**

В `index.html` найди `<div id="tabBar"` и добавь новую вкладку:

```html
<div class="tab-item" data-tab="properties">
  <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
  <span>Объекты</span>
</div>
```

В `app.js` в функции `switchTab` добавь:

```js
const tabScreens = {
  map: 'screen-map',
  properties: 'screen-properties',
  crm: 'screen-crm',
  base: 'screen-base',
  profile: 'screen-profile'
};
// И в обработчике:
if (tab === 'properties') AgentProperties.show();
if (tab === 'crm') AgentCrm.show();
```

- [ ] **Step 5: Проверить**

  Войди как агент → нажми вкладку "Объекты" → должен видеть пустой список.
  Нажми "+ Добавить" → откроется форма.
  Добавь фото с камеры → заполни адрес + цену → "Опубликовать".
  Объект появляется в списке со статусом "Опубликован".

- [ ] **Step 6: Commit**

```bash
git add projects/24streets-app/js/agent-properties.js projects/24streets-app/index.html projects/24streets-app/css/styles.css
git commit -m "feat: agent property management screen with photo upload"
```

---

## Task 6: CRM Kanban-воронка

**Files:**
- Create: `projects/24streets-app/js/agent-crm.js`
- Modify: `projects/24streets-app/index.html` (screen-crm + screen-lead-detail)
- Modify: `projects/24streets-app/css/styles.css`

**Interfaces:**
- Consumes: `Sb.getAgentLeads()`, `Sb.updateLeadStage()`
- Produces: `AgentCrm.init(profile)`, `AgentCrm.show()`

- [ ] **Step 1: Добавить screen-crm в index.html**

```html
<!-- ══════ SCREEN CRM (агент) ══════ -->
<div id="screen-crm" class="screen slide-below">
  <div class="ds-status-bar"></div>
  <div style="background:var(--s2);border-bottom:1px solid var(--border);padding:12px 16px;">
    <div style="font-size:16px;font-weight:700;color:var(--ink)">Сделки</div>
  </div>
  <div id="crmBoard" class="crm-board"></div>
</div>

<!-- ══════ SCREEN LEAD DETAIL ══════ -->
<div id="screen-lead-detail" class="screen slide-below">
  <div class="ds-status-bar"></div>
  <div class="ds-header" style="background:var(--s2);border-bottom:1px solid var(--border);">
    <div id="leadDetailBack" class="ds-back">
      <svg width="7" height="12" viewBox="0 0 7 12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><polyline points="6,1 1,6 6,11"/></svg>
    </div>
    <div id="leadDetailTitle" style="flex:1;font-size:16px;font-weight:700;color:var(--ink);margin-left:12px">Лид</div>
  </div>
  <div id="leadDetailContent" class="lead-detail-scroll"></div>
</div>
```

- [ ] **Step 2: Добавить стили CRM в styles.css**

```css
/* ══════════════════════════════════════════
   CRM KANBAN
══════════════════════════════════════════ */
.crm-board {
  flex: 1; overflow-x: auto; display: flex; gap: 12px;
  padding: 16px 12px; align-items: flex-start;
  scrollbar-width: none;
}
.crm-board::-webkit-scrollbar { display: none; }
.crm-col {
  min-width: 200px; flex-shrink: 0;
  background: #f5f3ef; border-radius: 14px;
  padding: 12px 10px;
}
.crm-col-title {
  font-size: 10px; font-weight: 700; letter-spacing: .08em;
  color: var(--ink2); text-transform: uppercase;
  margin-bottom: 10px; padding: 0 2px;
}
.crm-col-count {
  display: inline-block; background: white;
  border-radius: 6px; padding: 1px 6px;
  font-size: 10px; font-weight: 700; color: var(--ink);
  margin-left: 6px;
}
.lead-card {
  background: white; border-radius: 12px;
  padding: 12px; margin-bottom: 8px;
  border: 1px solid var(--border); cursor: pointer;
}
.lead-card:active { opacity: .85; }
.lead-card-addr {
  font-size: 13px; font-weight: 600; color: var(--ink);
  margin-bottom: 4px;
}
.lead-card-phone {
  font-size: 11px; color: var(--ink2); font-weight: 500;
}
.lead-card-time {
  font-size: 10px; color: var(--ink3); margin-top: 6px;
}

/* Lead detail */
.lead-detail-scroll {
  flex: 1; overflow-y: auto; padding: 16px;
}
.lead-stage-row {
  display: flex; gap: 8px; flex-wrap: wrap; margin-bottom: 20px;
}
.stage-btn {
  flex: 1; min-width: 80px; padding: 10px 8px;
  border-radius: 10px; font-size: 11px; font-weight: 700;
  border: 1.5px solid var(--border); background: white;
  color: var(--ink2); cursor: pointer;
  font-family: 'Inter', sans-serif; text-align: center;
}
.stage-btn.active { background: var(--blue); border-color: var(--blue); color: white; }
.stage-btn.past   { background: #f0f9f5; border-color: var(--green); color: var(--green); }
```

- [ ] **Step 3: Создать agent-crm.js**

```js
// js/agent-crm.js
const STAGES = [
  { id: 'new',       label: 'Новый лид' },
  { id: 'contacted', label: 'Контакт' },
  { id: 'showing',   label: 'Показ' },
  { id: 'deposit',   label: 'Задаток' },
  { id: 'deal',      label: 'Сделка' },
  { id: 'lost',      label: 'Отказ' },
];

const AgentCrm = {
  _profile: null,
  _leads: [],

  init(profile) {
    this._profile = profile;
    document.getElementById('leadDetailBack')
      .addEventListener('click', () => slideBack('screen-lead-detail', 'screen-crm'));
  },

  async show() {
    this._leads = await Sb.getAgentLeads(this._profile.id);
    this._renderBoard();
  },

  _renderBoard() {
    const board = document.getElementById('crmBoard');
    board.innerHTML = STAGES.map(s => {
      const leads = this._leads.filter(l => l.stage === s.id);
      return `<div class="crm-col" data-stage="${s.id}">
        <div class="crm-col-title">${s.label}<span class="crm-col-count">${leads.length}</span></div>
        ${leads.map(l => this._leadCardHTML(l)).join('')}
      </div>`;
    }).join('');

    board.querySelectorAll('.lead-card').forEach(card => {
      card.addEventListener('click', () => {
        const lead = this._leads.find(l => l.id === card.dataset.id);
        if (lead) this._openDetail(lead);
      });
    });
  },

  _leadCardHTML(l) {
    const addr  = l.properties?.address || '—';
    const phone = l.buyer_phone || l.buyer_profiles?.phone || '—';
    const time  = new Date(l.created_at).toLocaleDateString('ru');
    return `<div class="lead-card" data-id="${l.id}">
      <div class="lead-card-addr">${addr}</div>
      <div class="lead-card-phone">${phone}</div>
      <div class="lead-card-time">${time}</div>
    </div>`;
  },

  _openDetail(lead) {
    const addr  = lead.properties?.address || '—';
    const price = lead.properties?.price_label || '';
    const phone = lead.buyer_phone || lead.buyer_profiles?.phone || '—';
    const stageIdx = STAGES.findIndex(s => s.id === lead.stage);

    document.getElementById('leadDetailTitle').textContent = addr;

    const stageButtons = STAGES.slice(0, 5).map((s, i) => {
      const cls = i < stageIdx ? 'past' : (i === stageIdx ? 'active' : '');
      return `<button class="stage-btn ${cls}" data-stage="${s.id}">${s.label}</button>`;
    }).join('');

    document.getElementById('leadDetailContent').innerHTML = `
      <div style="font-size:22px;font-weight:700;color:var(--ink);margin-bottom:4px">${price} ₸</div>
      <div style="font-size:13px;color:var(--ink2);margin-bottom:20px">${addr}</div>
      <div style="font-size:10px;font-weight:700;letter-spacing:.08em;color:var(--ink3);text-transform:uppercase;margin-bottom:10px">Этап сделки</div>
      <div class="lead-stage-row">${stageButtons}</div>
      <div style="font-size:10px;font-weight:700;letter-spacing:.08em;color:var(--ink3);text-transform:uppercase;margin-bottom:8px">Покупатель</div>
      <div style="font-size:15px;font-weight:600;color:var(--ink);margin-bottom:16px">${phone}</div>
      <a href="https://wa.me/${phone.replace(/\D/g,'')}" target="_blank"
         style="display:block;text-align:center;padding:14px;background:var(--green);color:white;border-radius:12px;font-weight:700;text-decoration:none;font-size:14px">
        WhatsApp
      </a>
    `;

    document.querySelectorAll('.stage-btn').forEach(btn => {
      btn.addEventListener('click', async () => {
        await Sb.updateLeadStage(lead.id, btn.dataset.stage, null);
        lead.stage = btn.dataset.stage;
        this._renderBoard();
        this._openDetail(lead);
      });
    });

    slideForward('screen-crm', 'screen-lead-detail');
  },
};
```

- [ ] **Step 4: Проверить**

  Войди как агент → нажми вкладку "Сделки".
  Пока лидов нет — должен видеть 6 пустых колонок Kanban.
  После Task 7 (покупатель разблокирует контакт) появятся карточки.

- [ ] **Step 5: Commit**

```bash
git add projects/24streets-app/js/agent-crm.js projects/24streets-app/index.html projects/24streets-app/css/styles.css
git commit -m "feat: CRM kanban voronka with 5 deal stages"
```

---

## Task 7: Лента покупателя + Contact Gate

**Files:**
- Create: `projects/24streets-app/js/buyer-feed.js`
- Modify: `projects/24streets-app/index.html` (screen-buyer-feed)
- Modify: `projects/24streets-app/css/styles.css`

**Interfaces:**
- Consumes: `Sb.getPublishedProperties()`, `Sb.createLead()`, `Auth.showPhoneOTP()`
- Produces: `BuyerFeed.show()`

- [ ] **Step 1: Добавить screen-buyer-feed в index.html**

```html
<!-- ══════ SCREEN BUYER FEED ══════ -->
<div id="screen-buyer-feed" class="screen slide-below">
  <!-- Header -->
  <div class="buyer-feed-hdr">
    <div class="logo">24<span>Streets</span></div>
    <div style="font-size:11px;color:var(--ink2);font-weight:500">Алматы</div>
  </div>
  <!-- TikTok scroll -->
  <div id="buyerFeedWrapper" class="buyer-feed-wrapper"></div>
  <!-- Bottom nav покупателя -->
  <div class="buyer-tab-bar">
    <div class="buyer-tab on">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 00-2-2h-4a2 2 0 00-2 2v2"/></svg>
      <span>Лента</span>
    </div>
    <div class="buyer-tab" id="buyerProfileTab">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
      <span>Профиль</span>
    </div>
  </div>
</div>
```

- [ ] **Step 2: Добавить стили в styles.css**

```css
/* ══════════════════════════════════════════
   BUYER FEED
══════════════════════════════════════════ */
#screen-buyer-feed { background: var(--bg); }
.buyer-feed-hdr {
  position: absolute; top: 0; left: 0; right: 0; z-index: 200;
  display: flex; align-items: center; justify-content: space-between;
  padding: calc(var(--safe-top) + 10px) 16px 10px;
  background: var(--bg);
  border-bottom: 1px solid var(--border);
  height: calc(48px + var(--safe-top));
}
.buyer-feed-wrapper {
  position: absolute;
  top: calc(48px + var(--safe-top));
  left: 0; right: 0;
  bottom: calc(64px + var(--safe-bot));
  overflow-y: scroll; overflow-x: hidden;
  scroll-snap-type: y mandatory;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
}
.buyer-feed-wrapper::-webkit-scrollbar { display: none; }
.buyer-tab-bar {
  position: absolute; bottom: 0; left: 0; right: 0;
  height: calc(64px + var(--safe-bot));
  display: flex; background: white;
  border-top: 1px solid var(--border);
  padding-bottom: var(--safe-bot);
}
.buyer-tab {
  flex: 1; display: flex; flex-direction: column;
  align-items: center; justify-content: center; gap: 3px;
  font-size: 10px; font-weight: 600; color: var(--ink3);
  cursor: pointer;
}
.buyer-tab.on { color: var(--blue); }
.buyer-tab.on svg { stroke: var(--blue); }

/* Карточка покупателя (похожа на агентскую) */
.buyer-card {
  height: var(--feed-card-h, calc(100dvh - 52px));
  scroll-snap-align: start;
  background: white; border-radius: 20px 20px 0 0;
  overflow: hidden; display: flex; flex-direction: column; flex-shrink: 0;
}
.buyer-card-price-row {
  padding: 16px 16px 8px;
  border-bottom: 1px solid #f0ece5;
}
.buyer-card-price { font-size: 28px; font-weight: 700; color: var(--ink); letter-spacing: -1px; }
.buyer-card-photo {
  flex: 0 0 36%; background: var(--bg);
  display: flex; align-items: center; justify-content: center;
  overflow: hidden;
}
.buyer-card-photo img { width: 100%; height: 100%; object-fit: cover; }
.buyer-card-content { flex: 1; padding: 14px 16px; overflow: hidden; }
.buyer-card-addr { font-size: 18px; font-weight: 700; color: var(--ink); margin-bottom: 4px; }
.buyer-card-meta { font-size: 12px; color: var(--ink3); margin-bottom: 14px; }
.buyer-card-stats { display: flex; gap: 16px; margin-bottom: 16px; }
.buyer-stat-num { font-size: 22px; font-weight: 700; color: var(--ink); }
.buyer-stat-lbl { font-size: 10px; color: var(--ink3); }
.buyer-contact-btn {
  width: 100%; height: 48px; border-radius: 14px;
  background: var(--blue); color: white;
  font-size: 15px; font-weight: 700; border: none; cursor: pointer;
  font-family: 'Inter', sans-serif;
}
.buyer-contact-revealed {
  background: white; border: 1.5px solid var(--border);
  border-radius: 14px; padding: 14px 16px;
  display: flex; align-items: center; justify-content: space-between;
}
.buyer-contact-phone { font-size: 18px; font-weight: 700; color: var(--ink); }
.buyer-wa-btn {
  padding: 10px 16px; border-radius: 10px;
  background: var(--green); color: white;
  font-size: 13px; font-weight: 700; border: none; cursor: pointer;
  font-family: 'Inter', sans-serif; text-decoration: none;
}
```

- [ ] **Step 3: Создать buyer-feed.js**

```js
// js/buyer-feed.js
const BuyerFeed = {
  _props: [],
  _user: null,

  async show() {
    // Скрыть все остальные экраны
    document.querySelectorAll('.screen').forEach(s => {
      s.classList.remove('active');
      s.classList.add('slide-below');
    });
    const s = document.getElementById('screen-buyer-feed');
    s.classList.remove('slide-below');
    s.classList.add('active');
    document.getElementById('tabBar')?.classList.add('hidden');

    const session = await Sb.getSession();
    this._user = session?.user || null;

    this._props = await Sb.getPublishedProperties(100);
    this._render();
  },

  _render() {
    const wrapper = document.getElementById('buyerFeedWrapper');
    wrapper.innerHTML = '';
    wrapper.scrollTop = 0;

    if (!this._props.length) {
      wrapper.innerHTML = '<div style="height:100%;display:flex;align-items:center;justify-content:center;color:var(--ink2);font-size:14px">Нет объявлений</div>';
      return;
    }

    this._props.forEach(p => {
      wrapper.insertAdjacentHTML('beforeend', this._cardHTML(p));
    });

    // Биндим кнопки контакта
    wrapper.querySelectorAll('.buyer-contact-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const propId = btn.dataset.propId;
        const agentId = btn.dataset.agentId;
        this._handleContact(btn, propId, agentId);
      });
    });
  },

  _cardHTML(p) {
    const photo = p.photos?.[0]
      ? `<img src="${p.photos[0]}" alt="">`
      : `<div style="width:100%;height:100%;background:var(--bg)"></div>`;
    const meta = [p.building_type, p.rooms ? `${p.rooms} комн.` : null, p.area ? `${p.area} м²` : null, p.floor && p.floors ? `${p.floor}/${p.floors} эт` : null].filter(Boolean).join(' · ');

    return `<div class="buyer-card">
      <div class="buyer-card-price-row">
        <div class="buyer-card-price">${p.price_label || ''} <span style="font-size:16px;font-weight:600">₸</span></div>
      </div>
      <div class="buyer-card-photo">${photo}</div>
      <div class="buyer-card-content">
        <div class="buyer-card-addr">${p.address || '—'}</div>
        <div class="buyer-card-meta">${meta}</div>
        <div class="buyer-card-stats">
          ${p.rooms != null ? `<div><div class="buyer-stat-num">${p.rooms || 'С'}</div><div class="buyer-stat-lbl">${p.rooms === 0 ? 'студ.' : 'комн.'}</div></div>` : ''}
          ${p.area ? `<div><div class="buyer-stat-num">${p.area}</div><div class="buyer-stat-lbl">м²</div></div>` : ''}
          ${p.floor && p.floors ? `<div><div class="buyer-stat-num">${p.floor}/${p.floors}</div><div class="buyer-stat-lbl">этаж</div></div>` : ''}
        </div>
        <div id="contact-${p.id}">
          <button class="buyer-contact-btn" data-prop-id="${p.id}" data-agent-id="${p.agent_id}">
            Позвонить агенту
          </button>
        </div>
      </div>
    </div>`;
  },

  async _handleContact(btn, propId, agentId) {
    // Если уже авторизован как покупатель — показать контакт
    if (this._user) {
      await this._revealContact(btn, propId, agentId);
      return;
    }
    // Иначе — запросить телефон
    Auth.showPhoneOTP(async (user) => {
      this._user = user;
      await this._revealContact(btn, propId, agentId);
    });
  },

  async _revealContact(btn, propId, agentId) {
    // Получить телефон агента
    const { data: agentData } = await Sb.db
      .from('profiles')
      .select('phone, name')
      .eq('id', agentId)
      .single();

    const phone = agentData?.phone || '';
    const wa    = `https://wa.me/${phone.replace(/\D/g,'')}`;
    const container = document.getElementById(`contact-${propId}`);
    container.innerHTML = `
      <div class="buyer-contact-revealed">
        <div>
          <div style="font-size:11px;color:var(--ink3);margin-bottom:2px">${agentData?.name || 'Агент'}</div>
          <div class="buyer-contact-phone">${phone}</div>
        </div>
        <a href="${wa}" target="_blank" class="buyer-wa-btn">WhatsApp</a>
      </div>`;

    // Создать лид в CRM агента
    if (this._user) {
      try {
        await Sb.createLead(propId, agentId, this._user.id, phone || this._user.phone);
      } catch (e) {
        console.warn('Lead already exists or error:', e);
      }
    }
  },
};
```

- [ ] **Step 4: Проверить полный сценарий покупателя**

  1. Открой app без авторизации → должен видеть экран агентского логина
  2. Нажми "Смотреть объекты" → открывается `screen-buyer-feed`
  3. Если нет опубликованных объектов — текст "Нет объявлений"
  4. Опубликуй объект через агентский аккаунт (Task 5)
  5. Вернись в buyer-режим → объект появился в ленте
  6. Нажми "Позвонить агенту" → открывается phone-otp экран
  7. Введи телефон → получи SMS → введи код → видишь телефон агента + WhatsApp
  8. Войди как агент → вкладка "Сделки" → появилась карточка лида

- [ ] **Step 5: Commit**

```bash
git add projects/24streets-app/js/buyer-feed.js projects/24streets-app/index.html projects/24streets-app/css/styles.css
git commit -m "feat: buyer TikTok feed with phone OTP contact gate and lead creation"
```

---

## Task 8: Agency onboarding (Admin регистрация агентства)

**Files:**
- Modify: `projects/24streets-app/index.html` (screen-agency-setup)
- Modify: `projects/24streets-app/js/auth.js` (добавить agency setup flow)

**Interfaces:**
- Produces: агентство создано в БД, admin profile создан, invite-ссылки для агентов

- [ ] **Step 1: Добавить screen-agency-setup в index.html**

```html
<!-- ══════ SCREEN AGENCY SETUP ══════ -->
<div id="screen-agency-setup" class="screen slide-below auth-screen">
  <div class="auth-logo">24<span>Streets</span></div>
  <div class="auth-tagline">Регистрация агентства</div>
  <div class="auth-card">
    <div class="auth-title">Новое агентство</div>
    <div id="setupError" class="auth-error" style="display:none"></div>
    <input id="setupAgencyName" class="auth-input" type="text" placeholder="Название агентства">
    <input id="setupAdminName" class="auth-input" type="text" placeholder="Ваше имя">
    <input id="setupEmail" class="auth-input" type="email" placeholder="Email">
    <input id="setupPassword" class="auth-input" type="password" placeholder="Пароль (мин. 6 символов)">
    <button id="setupSubmit" class="auth-btn">Создать агентство</button>
    <div class="auth-divider">Уже есть аккаунт?</div>
    <button id="setupToLogin" class="auth-btn-secondary">← Войти</button>
  </div>
</div>
```

- [ ] **Step 2: Добавить в auth.js метод showAgencySetup и обработчики**

```js
// Добавить в объект Auth:
showAgencySetup() {
  document.querySelectorAll('.screen').forEach(s => {
    s.classList.remove('active'); s.classList.add('slide-below');
  });
  const s = document.getElementById('screen-agency-setup');
  s.classList.remove('slide-below'); s.classList.add('active');
},
```

Добавить в `Auth.bind()`:

```js
// Ссылка "Зарегистрировать агентство" на экране логина
// Добавь кнопку в screen-auth:
// <button id="authToSetup" class="auth-btn-secondary">Зарегистрировать агентство</button>
document.getElementById('authToSetup')?.addEventListener('click', () => this.showAgencySetup());
document.getElementById('setupToLogin')?.addEventListener('click', () => this.showAgentLogin());

document.getElementById('setupSubmit')?.addEventListener('click', async () => {
  const agencyName = document.getElementById('setupAgencyName').value.trim();
  const adminName  = document.getElementById('setupAdminName').value.trim();
  const email      = document.getElementById('setupEmail').value.trim();
  const password   = document.getElementById('setupPassword').value;
  document.getElementById('setupError').style.display = 'none';

  if (!agencyName || !adminName || !email || password.length < 6) {
    this._showError('setupError', 'Заполните все поля (пароль мин. 6 символов)'); return;
  }

  // 1. Создать пользователя
  const { data: signUpData, error: signUpError } = await Sb.auth.signUp({ email, password });
  if (signUpError) { this._showError('setupError', signUpError.message); return; }

  const uid = signUpData.user.id;

  // 2. Создать агентство
  const { data: agency, error: agencyError } = await Sb.db
    .from('agencies')
    .insert({ name: agencyName })
    .select().single();
  if (agencyError) { this._showError('setupError', 'Ошибка создания агентства'); return; }

  // 3. Создать profile (admin)
  await Sb.db.from('profiles').insert({
    id: uid, agency_id: agency.id, role: 'admin', name: adminName,
  });

  location.reload();
});
```

- [ ] **Step 3: Добавить кнопку "Зарегистрировать агентство" в screen-auth**

Добавить после `authBuyerBtn`:
```html
<button id="authToSetup" class="auth-btn-secondary" style="margin-top:6px">Регистрация агентства</button>
```

- [ ] **Step 4: Проверить**

  Нажми "Регистрация агентства" → открывается форма.
  Заполни данные → нажми "Создать агентство".
  Proверь в Supabase Dashboard: таблицы `agencies` и `profiles` — должны появиться записи.
  Перезагрузка → должен войти как агент-admin.

- [ ] **Step 5: Commit**

```bash
git add projects/24streets-app/index.html projects/24streets-app/js/auth.js
git commit -m "feat: agency registration flow for admin onboarding"
```

---

## Task 9: Интеграция с Telegram-ботом (уведомление о новом лиде)

**Files:**
- Modify: `projects/24streets-app/js/buyer-feed.js` (добавить Telegram notify после createLead)
- Create: `projects/24streets/inbox/notify_lead.py`

**Interfaces:**
- Consumes: лид из `leads`, Telegram Bot API, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID из `.env`

- [ ] **Step 1: Создать Supabase Database Function для уведомлений**

В Supabase SQL Editor:

```sql
-- Функция-триггер при INSERT в leads
CREATE OR REPLACE FUNCTION notify_new_lead()
RETURNS trigger AS $$
DECLARE
  agent_phone text;
  buyer_phone text;
  prop_addr   text;
BEGIN
  SELECT phone INTO agent_phone FROM profiles WHERE id = NEW.agent_id;
  SELECT phone INTO buyer_phone FROM buyer_profiles WHERE id = NEW.buyer_id;
  SELECT address INTO prop_addr FROM properties WHERE id = NEW.property_id;

  PERFORM pg_notify('new_lead', json_build_object(
    'lead_id',     NEW.id,
    'agent_id',    NEW.agent_id,
    'buyer_phone', COALESCE(NEW.buyer_phone, buyer_phone),
    'address',     prop_addr
  )::text);

  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_new_lead
  AFTER INSERT ON leads
  FOR EACH ROW EXECUTE FUNCTION notify_new_lead();
```

- [ ] **Step 2: Создать notify_lead.py**

```python
# projects/24streets/inbox/notify_lead.py
"""
Слушает Supabase Realtime и отправляет Telegram-уведомления агентам о новых лидах.
Запускать как отдельный процесс рядом с pipeline.py.
"""
import os, asyncio, json
from dotenv import load_dotenv
from supabase import create_client
import httpx

load_dotenv()

SUPABASE_URL = os.getenv('SUPABASE_URL')
SUPABASE_KEY = os.getenv('SUPABASE_KEY')
BOT_TOKEN    = os.getenv('TELEGRAM_BOT_TOKEN')
CHAT_ID      = os.getenv('TELEGRAM_CHAT_ID')

sb = create_client(SUPABASE_URL, SUPABASE_KEY)

async def send_telegram(text: str):
    url = f'https://api.telegram.org/bot{BOT_TOKEN}/sendMessage'
    async with httpx.AsyncClient() as client:
        await client.post(url, json={'chat_id': CHAT_ID, 'text': text, 'parse_mode': 'HTML'})

def on_lead(payload):
    data = payload.get('new', {})
    buyer_phone = data.get('buyer_phone', '—')
    text = (
        f"🔔 <b>Новый лид!</b>\n"
        f"📍 {data.get('buyer_phone', 'Покупатель')} заинтересовался объектом\n"
        f"📞 Телефон: <code>{buyer_phone}</code>\n"
        f"Откройте раздел Сделки в 24Streets"
    )
    asyncio.get_event_loop().create_task(send_telegram(text))

async def main():
    channel = sb.realtime.channel('leads-insert')
    channel.on('postgres_changes',
               event='INSERT', schema='public', table='leads',
               callback=on_lead)
    await channel.subscribe()
    print('Listening for new leads...')
    await asyncio.sleep(float('inf'))

if __name__ == '__main__':
    asyncio.run(main())
```

- [ ] **Step 3: Добавить в .env**

```
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_KEY=eyJ...anon key...
```

- [ ] **Step 4: Проверить**

  Запусти `python notify_lead.py` в отдельном терминале.
  Зайди в buyer-режим → разблокируй контакт на каком-либо объекте.
  Должно прийти Telegram-сообщение о новом лиде.

- [ ] **Step 5: Commit**

```bash
git add projects/24streets/inbox/notify_lead.py
git commit -m "feat: telegram notification on new lead via supabase realtime"
```

---

## Порядок выполнения

```
Task 1 (Supabase setup)
  → Task 2 (клиент в app)
    → Task 3 (auth screens)
      → Task 4 (role routing)     ← первый рабочий MVP
        → Task 5 (agent properties)
        → Task 6 (CRM kanban)
        → Task 7 (buyer feed)     ← второй рабочий MVP
          → Task 8 (agency onboarding)
          → Task 9 (telegram notify)
```

Tasks 5, 6, 7 можно делать параллельно после Task 4.
